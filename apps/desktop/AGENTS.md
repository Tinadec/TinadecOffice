# DESKTOP APP KNOWLEDGE

**Last Updated:** 2026-09-22
**Last Updated By:** 阶段 6 第二段：记忆评审面（`pages/MemoryPage.vue`、`/memory`、候选依据/适用条件/失效条件上屏），并第一次用测试断言"晋升的记忆真的进了模型上下文"；同日第一段是 run 的证据清单进 `OrchestrationTab` 并按真实投影重写 `ContextPackDto`，前面是阶段 4 快照逐文件评审、阶段 5 工作区技能索引、命令面板鼠标入口
**Last Verified Commit:** `424b12b`（docs(desktop)：行尾归一记录）之后的工作树
**Branch:** Everything-changed

## OVERVIEW
Electron + Vue 3 desktop app. Vite renders the UI; Electron provides the window/preload bridge; renderer talks to Gateway only.

TinaChat（2026-09-19，2026-09-20 收口为纯观察）：定位是**智能体的交流频道**——人类侧只有观察与搭架构两种动作，聊天室既没有发送框也**没有任何写操作**（人跟 AI 说话走 Home 的正常对话界面；简报的采纳与交接属于会话角色，由智能体用自己的工具完成）。两个面分别落在：
- 观察位：侧边栏“聊天室”`/chatroom` = `components/tinaChat/ChatroomPanel.vue` + `tinaChat/useChatroomObserver.ts`，只调 4 个管理员 observer GET；显示群聊/私聊、作者/完整受众/来源、意图卡片与 ACK（确认接收≠已理解），默认 5 秒轮询、后台/卸载暂停、历史分页保留滚动位置、切换会话丢弃迟到响应、401/403 清空受保护内容。管理员范围由 Core 判定，UI 不自授权限，也不入群、不替智能体 ACK。**`ChatroomPanel.test.ts` 的 "Chatroom stays read-only" 用例是这条边界的守卫**：它对 proposed/accepted 两种状态断言不存在任何 `intent-accept|reject|execute|mode|project|desk|notice` 控件、按钮文本不含"采纳/拒绝/交接"、且 decide/execute API 从未被调用——往面板加回任何写操作都会让它红（已用注入变异验证过）。
- 曾经的裁决位（**已删除**）：`tinaChat/useIntentDesk.ts` 让人类在简报卡上采纳/拒绝并选模式交接。规范 §14.4 只写"owner/admin 按会话 revision 采纳"，从不要求是人类，代码 `RequireAdmin(member)` 也不看 kind；把人类塞成必经节点既违背定位又让智能体闭不了环，故整体移除（含 5 条用例与 5 个 desk-only 文案键）。
- 架构面：设置页“智能体交流”子页 `settings/sections/TinaChatSection.vue`：注册/停用参与者身份、以某一身份建群与邀请/移除/任命、读写跨工作区通信策略。身份提示文案不再暗示"必须注册一个用户身份"（实测 agent 身份 `POST /conversations` 返回 201，人类不是建群前提）。`api.ts` 补齐 20 个非观察操作，`/api/v1/tina-chat` 实为 **24 操作 / 17 路径**（旧记录写 25 与“21+4”，均与源码不符；以 `Map*` 计数与 Core OpenAPI 快照为准）。
- “我的身份”存在 localStorage `tinadec.tinachat.identities`（`tinaChat/useChatIdentity.ts`），只是省得每次重选 `actor_id`；归属由 Core 逐次核验，写错只会 403，不会提权。
- 2026-09-20 真机走查（真 Electron 43.3.0 + CDP + 真模型）实测：GraphSeedPack 升级对话框正确识别 2.2.0→2.4.0 并核对 digest；设置页注册三个身份、建群、邀请→本人接受全通；聊天室纯观察正常。**发现的缺陷**：① 审批在功能面板可点，但**对话里没有任何工具卡与审批入口**（`MessageList.vue:35` 把工具卡按消息 id 挂载，首轮 run 还没有 assistant 消息 → 无落脚点），用户只看到 "awaiting user" 和转圈；② 审批行**不显示工具名/命令/风险/归属**，无法据以判断；③ 注册身份默认 `receive_human_messages=false`，人类消息因此静默不唤醒任何整理者，界面无解释。未做且不得写成已完成：实时推送（仍是轮询）、成员在线状态、附件、访客问答。
- **缺陷① 已修（2026-09-21）**：`MessageList.vue` 的契约改成两个互不重叠的输入——`activityByMessage`（按消息 id 挂载，run 收尾且有 assistant 消息时）与 `liveTurn`（跟随最后一个尚未落地的活动块常驻渲染），归属判定放在 `ChatPanel.vue`：`busy` 或缺少 assistant 落点时活动属于 `LiveTurnBlock`，否则归最后一条 assistant 消息。**不要退回旧写法**：`props.thinkingSteps ?? (role === 'assistant' ? thinkingSteps : undefined)` 会把同一份实时活动盖到**每一条** assistant 消息上，且首轮永远无处可挂。`chat/LiveTurnBlock.vue` 用扁平无边框无背景的聊天视觉语言（状态只由字形颜色表达），转发 approve/reject 到与功能面板同一个审批入口；`MessageList.test.ts` 4 例钉住"首轮无消息也有工具卡与审批按钮""run 结束后活动归位到最后一条消息""两处状态切换都不重复渲染"，已用变异验证（去掉 liveTurn 分支即红）。
- **消息级「编辑重发」接通（2026-09-21，阶段 2b）**：`MessageItem.vue` 的铅笔按钮一直存在，但 `saveEdit()` 是 `// TODO: emit edit event` 的空壳——点保存只关掉编辑框，历史一字未改，下一轮 run 继续按被改掉的那句话推理；Core 侧此前也没有"把某条消息之后移出会话"的能力。**出货面的真相**（第一次找错地方就会白干）：对话面不在 `src/pages/`，而是 `apps/TinadecUI/src/components/cards/home/ChatCard.vue` 直接 `import ChatPanel from '@/components/ChatPanel.vue'` 并把 emit 接回 `homeController` 模块单例；所以**一个新对话动作要同时改四处**——`MessageItem`（发事件）→ `MessageList`（转发）→ `ChatPanel`（对外事件名）→ `ChatCard`（接 controller）。`grep ChatPanel src/pages` 查不到消费者 ≠ 死代码。落点选择：**不自动重发**。`handleSend` 的失败被 `run()` 吞掉（只 notify，不抛），控制器无法判定重发是否落地；而 revert 是不可逆的那一半，先切断再丢文本就是破坏性编辑。所以 `HomeController.editAndResend` 两条出口都把修正后的文本交回输入框（成功=切断+回填，Core 409 拒绝=不切断+回填+说明），且**输入框非空时整个操作中止**——两段未发送的文本永不相撞。`pending-` 开头的乐观行不给编辑入口（那个 id Core 从没见过）；清空文本等于取消，不切断。`chat.copy`/`chat.edit`/`common.cancel` 此前在两个包里都不存在，编辑行的按钮在真机上直接渲染键名；确认键改用 `chat.applyEdit`（"保存"会承诺一次并不存在的持久化——切断后落定的是输入框）。`i18nParity.test.ts` 的 `panelSources` 已登记 `MessageItem.vue` + `MessageList.vue`（`ChatPanel.vue` 不登记：它一个键都不引用，登记会让"至少引用一个翻译"的断言恒红）。**验证**：`MessageList.test.ts` 3 新例（含确认按钮自报 `chat.applyEdit` 的读回）+ `HomeController.test.ts` 3 新例 + parity 全绿。**未验证**：真 Electron 走一遍编辑→切断→回填→重发；被 revert 的消息作为 `trigger_message_id` 时 run 恢复的真实行为。
- **缺陷② 已修（2026-09-21，跨层）**：审批此前只给 `tool_id` + 一行 summary，策略驻留行的 summary 还是写死的 `"Tool permission request requires authorization."`，人类等于盲签。Core 侧新增 `ApprovalEvidenceProjector`（铸造即冻结的脱敏投影：`command`/`cwd`/`resource_path` + 有界 `arguments` 摘要，bulk 值换成"N chars, sha256 …"、secret 形状键整个丢弃），`ApprovalResponseDto` 从此多这四个字段，`/api/v1/approvals?status=pending` 的两类行（permission 与审批行）都能带出事实——permission 包络本身没有参数，靠 `tool_executions.permission_request_id` 的一次批量 join 取回（`ToolDispatcher` 在驻留**之前**就绑好了）。细则与"为什么这条链上人类裁决的是 permission 行"见 `TinadecCore/AGENTS.md` 的 APPROVAL EVIDENCE 段。桌面侧：`api.ts` 的 `ApprovalDto` 补 `tool_id`/`risk`/`arguments`/`resource_path`（**这是手写类型**：`/api/v1/approvals*` 在 OpenAPI 快照里只有路径没有响应体 schema，`check:drift` 抓不到它的漂移，字段回归由测试钉），`ApprovalTab.vue` 一行审批现在渲染工具名/风险（`data-risk` 只靠 `--accent-warning`/`--accent-danger` 字形着色）/命令行+工作目录/目标路径/可展开的脱敏参数，命令与路径**换行而不省略**（行内单行 ellipsis 会把要裁决的东西正好藏掉），旧行（digest 为空）整块不渲染而不是显示"没有命令"。`ApprovalTab.test.ts` 4 例覆盖三态 + run 作用域放行按钮仍只对 `kind==='permission'` 出现。

- **输入框斜杠命令（2026-09-21，阶段 2c）**：`/stop`、`/new`、`/queue <文本>`、`/parallel <文本>`，纯逻辑在 `src/lib/composerCommands.ts`（`parseComposerCommand` 三态：`run` / `incomplete` / `not-a-command`），弹层在 `ComposerBar.vue` 内用**本文件既有的 Teleport + computeDropdownPlacement** 模式（`role="listbox"`/`role="option"`/`aria-selected`，↑↓ 循环、Tab 补全、Esc 只关弹层）。两条硬规则：① 命中的命令**绝不以文本形式落到模型**——半截的 `/queue` 按 Enter 是补全成 `/queue `，不是把 "/queue" 当提示词发出去；只有匹配不上任何命令的斜杠散文（`/etc/hosts 里这一行…`）才走正常发送。② `/queue`、`/parallel` 是**一次性 dispatch 覆写**，不调 `setDispatchPref`——用户输入的"这次怎么办"不该改写他持久的回车偏好。draft 通过 `homeController.updateDraft` 直写而不是等两层 emit 冒泡，因为 `sendMessage` 紧接着就读它。`/stop` 只在 `canStop` 时才出现在候选里。**把 ComposerBar.vue 登记进 `i18nParity` 的 panelSources 后，守卫立刻抓到模板里 7 处硬编码中文**（`选择目标 run`、`确认引导`、`引导`、`并列`、`编辑`、`排队发送`、`并列发送`）——英文界面下它们就是中文，已全部迁到 `composer.*`；顺带给 `×` 移除按钮补了 `aria-label`（图标按钮此前无可访问名）。**同期查明、明确未做**：`add-image`/`add-file` 两个事件**全仓无监听者**，而 Core 根本没有承接客户端字节的端点（`IContentStore` 写入点全是 Core 内部产物，mediaType 只有 json/text；`MemoryDbContext.cs:181-205` 消息行无附件字段），`TinadecGateway/src/index.ts:2192` 的 `/api/v1/files/:sessionId/*` 代理到 Core **不存在**的路径 = 死路由；所以"附件"是一张新阶段垂直切片（本地取字节已有 `preload.cjs` 的 `selectBackgroundFile`/`readImageAsDataUrl` 可复用），不是这里能顺手接上的线。**（2026-09-21 后续：这段判断的"Core 没有端点"与"死路由"两句已经过期——Core 侧 `AttachmentEndpoints.cs` 已落地、Gateway 已用五条 `attachments` 路由替掉那条死路由、`api.ts` 已有 `uploadAttachment`/`listAttachments`/`attachmentContentUrl` 等封装（`attachments.test.ts` 7 例钉住线格式）。仍然成立的是：当时一个监听器都没有。选择器与附件条已由 2f 接上，剩"绑到消息、让模型看见"。）**另：`api.ts:2343` 的 `list_directory` 与 `:2345` 的 `glob_search` 是**不存在的工具 id**（真名 `ls` / `file_search`，错 id 必返 404 `tool_not_found`），@ 提及要用它们——**下一步已做**：2d 第一段就把这些包装纠成真实 id，2e 又给"按不存在的 id 分支"加了单一 owner 与全仓字面量守卫。**验证（本轮亲手跑）**：`composerCommands.test.ts` 7 例 + `ComposerBar.test.ts` 5 新例 + parity 全绿；**未验证**：真 Electron 里弹层的视觉排版（grid/min-width 只是按既有菜单令牌写的）。

- **Code 页把工具清单当成想象中的那份（2026-09-21，阶段 2d 第一段：请求侧与响应侧一起纠偏）**。**请求侧**：`api.ts` 的语义包装里 `list_directory`、`glob_search`、`grep_content` 在 TinadecTools 中**不存在**（真实 id 由 `[ToolFunction("…")]` 决定：`ls`、`stat`、`read_file`、`write_file`、`file_search`、`command_run`、`git_*`、`mcp_*`、`insert_*/replace_*/delete_*`），Core 对未知 id 直接 404 `tool_not_found`；`read_file` 的参数键也写错（`path`/`start_line`/`end_line`，真值 `filepath`/`start_row`/`end_row`，FileReader.cs:14-16）。`globSearch` 已**删除**——清单里没有按文件名搜的工具，留着它就是在假装能力。**响应侧同样是被猜的键**，而且更隐蔽（HTTP 200 + 空面板）：`ls` 给 `entries[].{name,path,type,size,modified_at}`（FileSystemTools.cs:31/35，`type` 是 `directory|file|link` 字符串，**没有 `is_dir`**），所以文件树把所有目录都渲染成不可展开的普通行，根以下整棵树都是死的；`file_search` 给扁平 `lines[]` + `file_hashes`（FileSearch.cs:59/79/87/91，**没有 `matches`**），所以两个搜索框永远显示"没有结果"。Core 是原样转发的（DirectToolEndpoints.cs:89 `response.Result?.Clone()`），因此这类漂移类型系统与 `check:drift` 都抓不到。修法：`src/lib/workspaceSearch.ts` 定为**读侧适配层**——`toDirEntryView`（`type`→`isDir`）、`searchedFilePaths`（直接取工具自己的 `file_hashes` 键，它已经去过重，不再客户端 dedupe）、`groupSearchLines`（把命中行与上下文行按文件重组，模板沿用既有 `.search-result-match-line`/`.search-result-ctx-line` 令牌）、`stripSearchRoot`（当时以为只需剥 `./` 前缀；**这条假设后来被实测推翻**，见第三段）。顺带纠出两处假开关：Regex 按钮此前**从未上线**（真键是 `fixed_strings`），现在 `fixed_strings: !useRegex`；Files 模式输入框提示 `glob: **/*.ts` 换成实话。`debug/preview` 的 mockApi/mockData 同步到真实形状与真实 id（否则预览里仍按旧键演一遍假成功）。**同批查明、明确未修**（#14）：`CodeEditor.vue:77`、`CodeViewer.vue:52` 走 `codeEditorOpen` → 不存在的 `code_editor`，所以**在 Code 页打开文件必然报错**；换成 `read_file` 前必须先确证 `all_contents[]` 每项的确切键名大小写——`LineContent` 是 positional record struct、无 `[JsonPropertyName]`，而 `FileReaderJsonContext` 未设命名策略（TinadecTools 里只有 ShellTool.cs:263 设了 SnakeCaseLower）。**当时写下这句话时"跑不了 provider"是个错误前提**：缺 rg 只是 MSBuild 的 warning，构建与运行都不受影响，下一段就把这件事实测清楚了。另有四处同类漂移：`ToolCatalogBrowser.vue:68`、`ToolExecutionTimeline.vue:48`、`ToolInvocationCard.vue:40`、`ToolResultViewer.vue:45` 的分支 id 全来自那份想象清单，图标恒走兜底、结构化结果视图恒不启用；真实写面是需审批且要求 `file_hash` 的 `write_file`，不是 `apply_patch`/`code_editor`。

- **输入框 `@` 路径补全（2026-09-21，阶段 2d 第二段）**：走**逐段 `ls`**，不做模糊全仓搜索——清单里只有 `ls` 能列目录，没有文件名索引，所以这不是"我们暂时没做好"而是"能力边界写死在实现里"。纯逻辑在 `src/lib/fileMentions.ts`：`parseMentionToken`（行首/空白/`(`/`[` 之后才算提及起点，`a@b` 是邮箱不是路径；片段里出现空白即结束）、`filterMentionEntries`（前缀优于子串、上限 8、目录与文件同列）、`completeMentionToken`（目录补 `/` 并留在原地继续下一段，文件补一个空格且**不重复加空格**）。弹层复用 2c 的 `computeDropdownPlacement` + `.composer-commands-portal` 表面令牌（只覆写行网格），Tab/Enter 都是**补全**、都不发送——半截的 `@src/ma` 按 Enter 不该变成一条发出去的消息；Esc 只关弹层，且**只对当前片段有效**，下次出现 `@` 重新展开。列目录要有工作区：`cwd` 只从 `props.projects` + `selectedProjectId` 的 `ProjectDto.path` 拿，**没选项目就一个字都不问**（没有工作区根就没有可列的对象，问出去也只是把一次点击变成一条错误通知）。两条本轮被自己的测试抓出来的真缺陷，别退回旧写法：① `refreshMentions` 原来只挂在 `modelValue` 的 watch 上，而**预填的 draft（编辑重发回填、恢复会话）从不触发变更**，于是整条补全对回填文本里的 `@` 完全不工作——现在 `onMounted` 也跑一次；② `parseMentionToken` 早期写成"取 `@` 前一个字符再判空白"，行首 `@` 因前面没有字符而被判为不是提及，`@src` 这种最常见写法反而补不出来。`fileMentions.test.ts` 12 例 + `ComposerBar.test.ts` 4 例（含"Enter 不发送""逐段下降：`@src/` 用 `ls src` 再列一次"）。**未验证**：真 Electron 下的弹层排版与光标跟随（`ls` 的真实响应已在第三段量到）。

- **把 provider 接上来量，而不是猜它（2026-09-21，阶段 2d 第三段：文件打开 + 路径方言）**：前两段留下一个"跑不了 TinadecTools 所以无法确证"的判断——**那是错的**：`TinadecTools/Tools/Search` 缺 rg 只是 MSBuild warning（`dotnet build tests/TinadecTools.Tests` 6 秒成功、0 错误），provider 用 `dotnet` 构建产物 `TinadecTools/bin/Debug/net10.0/TinadecTools.exe` 直接起得来。协议是**行分隔 JSON**（`ToolDispatchLoop.cs:50`）：请求 `{tool_id, session_id, toolcall_id, approved, params}` → 响应 `{call_id, success, result}`，**可写根 = 进程 CWD**（`FileToolRuntime.cs:23` `Environment.CurrentDirectory`），所以起子进程时把 cwd 指到临时目录就得到一个隔离的真实靶子。据此量到四件此前只能猜的事实：① `ls` 的 `entries[].path` 是**工作区相对**（`inner/deep.txt`）；② `file_search` 的 `filepath` / `file_hashes` 键是**绝对路径 + 反斜杠**（`C:\\tmp\\...\\note.txt`），不是我此前假设的 `./` 前缀；③ `read_file` 的 `all_contents[].content` **保留 C# 成员名 PascalCase**（`{"content":{"Content":"alpha","LineNumber":1,...},"line_hash":"1|JN"}`），`file_hash` 是 4 字符短摘要；④ `file_search` 真机可用（rg 在 PATH 上），`context_lines:1` 会交错返回 `is_match:false` 的上下文行——分组渲染那套假设成立。据此改掉三处：`stripSearchRoot` 换成 `toWorkspaceRelative(path, cwd)`（绝对→工作区相对，归一 `\`→`/`，认不出的路径原样返回而不是瞎猜），两个搜索面板与树内搜索都传 `props.cwd`；新增 `readFileText` 读 `all_contents[].content.Content`。**并修掉一条出货面必坏的链**：`CodeEditor.vue` / `CodeViewer.vue` 此前调 `codeEditorOpen`→不存在的 `code_editor`，在 Code 页打开任何文件都必然弹 "Failed to load file"；现在 `read_file`（文本 + `file_hash`）与 `stat`（`entry.size` / `modified_at`，read_file 不给这些）并行取。**顺手关掉一个并发写漏洞**：旧 `resolveFileHash()` 在**保存那一刻**再读一次盘算 hash，于是 `write_file` 的冲突校验永远通过——编辑器缓冲基于旧内容也能覆盖掉盘上的新内容。现在 hash 在加载时冻结进 `loadedFileHash`，保存后 `finishSave` 重新 `loadFile()` 取回新 hash（也顺带让 size/mtime 来自盘而不是"我以为我写了什么"）。死掉的 `apply_patch` / `code_editor` 包装（api.ts 与 debug/preview 两处，全无消费者）一并删除，`api.ts` 留一行注释指明真正的受治理写面是 `createUserToolActionForPath(cwd, 'write_file', {filepath, content, file_hash})`。**已知代价**：`read_file` 的载荷里根本没有"文件末尾是否有换行"，逐行 join 会丢掉结尾换行——保存往返可能少一个 `\n`，这是 provider 的边界，已在 `workspaceSearch.ts` 顶部写明，不在客户端伪造。`workspaceSearch.test.ts` 的读侧夹具是**真机原始 JSON 的抄件**（含 PascalCase 与反斜杠绝对路径），改错形状立刻红。**仍未验证**：Monaco 实际渲染（`CodeEditor`/`CodeViewer` 未做组件级用例）。

同一把靶子还能问出**权威工具清单**：`tool_id: "#manifest"`（`ToolRegistry.cs:25`）返回 **49 个 id**（protocol v2，`manifest_hash aab13ada12a6ef0385ddd7b8faf2f68fb50fd7874a50e91d611fbd9ad4712713`）+ 每个的 `requires_approval` / `mutates_workspace` / `risk` / `retry_safety`。上一段我写的"43 个"是错的，本轮重跑同一个探针才拿到确定值——**别把这段里的数字再抄一次，问靶子**。另外记一条协议坑：`toolcall_id` 是 **Int64**，传字符串会得到 `{"call_id":-1,"success":false,"error":"The JSON value could not be converted to System.Int64"}`（不是工具错误，是反序列化失败）。据此纠正一条我此前说错的判断：**`git_worktree_manager` 不是想象出来的**——它是 Core 的门面 id（`DirectToolEndpoints.cs:57`），把 `status` / `diff_preview` / `diff_compare` / `push_plan` / `branch_list` / `worktrees` / `log` 七个 action 路由到真实 `git_*` 工具，所以 `api.ts` 的 `gitDiffCompare` / `gitLog` 是有效调用。真正不存在的只有 `code_editor`、`apply_patch`（本轮已删）和 `sandbox_exec`（跑命令的真名是 `shell`，sandbox 一侧只有 `sandbox_status` / `sandbox_reset`）。清单里的写面几乎全部 `requires_approval + mutates_workspace`：`write_file`、`insert_line/byte/bytes`、`replace_lines/bytes`、`delete_line/bytes`、`command_run`、`shell`、`git_*` 变更类、`sandbox_reset`——它们走 `executeCodeTool` 只会拿到 202 `blocked`，必须走 `user/tool-actions`。**例外是 `mcp_invoke`**：`requires_approval:true` 但 `mutates_workspace:false`、`retry_safety:safe`，我上一段把它一并写成"写面"也是错的。全 49 个 id 的 `risk` 只有 `low` 与 `high` 两种取值。

- **工具展示层只认一个 owner（2026-09-21，阶段 2e）**：`ToolCatalogBrowser.vue` / `ToolExecutionTimeline.vue` / `ToolInvocationCard.vue` / `ToolResultViewer.vue` 各自复制过一份 id→图标链，四份都按 `list_directory` / `glob_search` / `grep_content` / `apply_patch` / `code_editor` / `sandbox_exec` 分支——**这些 id 一个都不存在**，于是每一条真工具都落到 `Wrench` 兜底。按不存在的 id 分支比不分支更糟：它看起来像审过、养着一堆 import，而真实工具永远拿默认样式。现在统一到 `src/lib/toolPresentation.ts`：`PROVIDER_TOOL_IDS`（49 个实测 id）、`CORE_VIRTUAL_TOOL_IDS`（对齐 `CoreVirtualToolPolicy.cs:12,23,48-53`：`create_workspace`、`task_dispatch`、九个 `tina_chat_*`）、`GIT_FACADE_TOOL_ID`，以及 `toolKindOf` / `toolIconOf` / `isKnownToolId` / `riskToneClass` / `riskLevelsPresent`。同一次改动里修掉两个同族缺陷：① **风险筛选器永远筛不出任何东西**——`riskOptions` 硬编码 `read-only`/`workspace-write`/`shell`/`git-write`/`external-url` 并按子串匹配，而 risk 是 `low`/`high` 这种等级，五个选项每一个都把目录清成空表；改成和同文件 `sourceOptions` 一样的**从当前行推导**，比较换成整值匹配。② `riskClass`/`riskColor` 在同一串假词上匹配，真实值全落默认灰；现在按等级上色，并把 `elevated` 用 `--accent-recovery` 令牌替掉了裸 `#bc8cff`。`ToolResultViewer.vue` 里那四个"结构化结果视图"（文件列表 / grep 命中 / diff / shell 输出）**根本不可能触发**，已连同 142 行 CSS 删除，依据不是"看着不像会用"而是量到的两点：`evidence` 装的是治理标记行而不是路径或补丁文本（`DirectToolEndpoints.cs:205,244,262` → `state_owner: core` / `approval_path: user_tool_action` / `provider_tool_id: <id>` / `git_action: <action>`），`checkpoint_summary` 由 Core 唯一的产生点写死为 `string.Empty`（`DmaeaEndpoints.cs:793,822`），全仓也搜不到 `diff --git`。`ToolExecutionTimelineItemDto` 本来就没有结果载荷字段，所以诚实的边界是"这一层不解析载荷"，而不是换个正则继续猜。`debug/preview/mockData.ts` 里 59 处假 id 一并改成真 id（`glob_search`+`grep_content` 塌成同一个 `file_search`，目录行去重；`replace_lines` 按清单改为 high + requires_approval），预览面板从此展示的是构建真能给出的东西。`toolPresentation.test.ts` 19 例，其中一例**扫全 `src/`**：只要这六个假 id 再以任何引号/反引号形式出现就红（本轮它抓出 53 处，含我自己上一轮写在注释里的反引号）。**已知残留**：扫描只认字面量，拼出来的 id 逃得过；`toolCatalog.ts` 的 `riskRank` 仍按那套假词排序（只影响顺序、`?? 10` 兜底 + `localeCompare`，不是坏界面，改它要连带重写一份通过中的夹具，价值不划算，留作已知债）。**未验证**：真 Electron 下目录/时间线的观感。

- **Debug Studio 的永久假控件剪掉了：那条 socket 三处都通不了（2026-09-21，阶段 3b 第二段：死传输面清理）**：`useDebugWebSocket` 拨的是 `ws://<gateway>/api/v1/debug/ws`。三层各自独立地让它不可能连通，都是量出来的：① Gateway 只把 **`/ws/debug`** 注册为 ws 路由（`TinadecGateway/src/index.ts`），客户端写的这个路径不在其中；② 即使走对路径，`/ws/debug` 也只代理到 Core 的 `/api/v1/debug/ws`（`src/websocket.ts`）；③ **Core 里没有任何 WebSocket 服务端代码**——对 `TinadecCore` 的 `.cs` 全量搜 `WebSocket` 零命中。于是 `onMounted` 每 3s 重连一次，每次以稳定 key 原地刷一条「WebSocket connection error」，而 `ws.messages` 一个读者都没有，标题栏那颗 LIVE/OFFLINE 药丸只能显示「离线」。
  - **四个模拟器按钮是同一族**：`useSimulation` 从不写 `mode`——唯一的写点是 DebugStudio 里 `@pause` 那句 `simulation.mode.value = 'paused'`，而那个按钮只有 `mode === 'running'` 才 enabled，所以它自己永远点不到。结果 step/run/pause 永久灰着；reset 是唯一可点的，而它发的是 `simulation.reset` 到那条开不了的 socket 上；`totalSteps` 恒为 0，`v-if="totalSteps > 0"` 那行进度文本因此从未渲染过。这不是「接上就活」的半成品，是三颗永久禁用的按钮加一颗永久离线的灯。
  - **删掉清单**：`useDebugWebSocket.ts` 整个文件、`WsMessage`/`WsEventType`（描述的是一份没有服务端的协议）、`SimulationMode` 与 `useSimulation` 的 `mode/currentStep/totalSteps`（唯一读者就是上面那些控件）、药丸及其 `.ws-status` CSS、SimulatorBar 的四个按钮与模式徽标，以及随之零消费者的 8 个键 `debugStudio.{live,offline,step,run,pause,reset,stepProgress,injectToolResult}`（两份 bundle 同步删，`i18nParity` 的「两份键集相等」断言因此仍绿）。
  - **有意留下的是那两条 REST**：展开区的「注入消息 / 强制审批」没删——它们打的是真实注册过的 `/api/v1/debug/simulate/*`，Core 目前逐条回 `501 NOT_IMPLEMENTED`（`StubEndpoints.MapDebugStubs`），点下去得到的是一条如实的错误。区分标准写在这里：**路径不存在的东西没有「以后」，501 有。**
  - **新防线 `src/debug/debugSurface.test.ts`**（三条结构断言，各自单独变异验证）：渲染层不许出现 `new WebSocket(`；子组件 `defineEmits` 声明了却从不自 `emit` 的事件不算数；父组件 `@x=` 必须命中子组件声明的事件。后两条必须**分开** A/B：一次注入两处变异时第三条竟然绿了——第二条补的 `'step': []` 恰好把第三条的孤儿绑定洗白，一条覆盖不了另一条。
  - **顺带量到的更大事实（本轮没动）**：整个 Debug Studio 是空壳。它那 6 个 GET 全部落在 `StubEndpoints.MapDebugStubs` 里返回空集合（`/debug/traces` → `{items:[],total:0}`、`/debug/spans` → `[]`、`/debug/metrics` → `{buckets:[]}`，`diagnostics`/`processes`/`breakpoints` 同理），所以时间轴 / Agent 图 / 指标 / 诊断四个标签页在任何真实后端下都只会显示「无数据」。要让它们有意义，前提是把 Core 侧 MAF 的 Activity/OTel 落成可查询存储——那是独立阶段，清理解决不了。
  - **门禁**：desktop `npm run test` EXIT=0、`vue-tsc` 仍是 **24** 条存量错、`vite build` EXIT=0。**未验证**：改完的 Debug Studio 没在真 Electron 里打开过（动的是模板与 CSS，最坏情况是底部那条岛的空隙看着别扭）。

- **Git 面板里两个“看起来在动”的控件其实一直空转（2026-09-21，存量类型债第一段）**：`vue-tsc` 那 24 条错里有 10 条集中在 `GitChangesView.vue`，它们不是“类型不够干净”，而是三个用户可见缺陷的现场记录。**修完 24 → 14。**
  - **Staged 分区永远是空的**：模板按 `f.is_staged` 过滤，而 `GitStatusFile` 根本没有这个字段（真值在 `staged_status` / `unstaged_status`）；同时 `repoSummary` 拿这两个字段去比 porcelain 字符（空格与问号），但 mint 点 `TinadecTools/Tools/Git/GitReadTools.cs` 里的 `StatusLabel` 发的是**词**（`clean` / `modified` / `untracked`…）。结果头部把每一行都数成 staged、分区把每一行都塞进 unstaged——两处互相矛盾，且都错。
  - **判定收成一个 owner**：新增 `src/lib/gitStatusSides.ts`（`isStagedFile` / `isUnstagedFile`，结构型入参，因为 `lib` 不该 import `composables`），`useGitOperation` 与 `GitChangesView` 都改读它；未跟踪行显式归入 Changes，否则它两边都不出现。`gitStatusSides.test.ts` 14 例：标签表是**从 mint 点文件里 parse 出来的**，数据按 `ParseStatusEntry` 的形状构造（与 `pendingAttachments.test.ts` 同一手法），外加一例穷举所有列组合，断言“provider 报出来的行不会两边都不在”。三条变异各自单跑都红：从 NO-CHANGE 集去掉 `clean`、去掉 untracked 分支、让 staged 判定不看 summary 列。
  - **“当前打开的是哪一行 diff”的高亮全废**：模板传 `selectedDiffFile?.path`，而 `selectedDiffFile` 本身就是 `ref<string | null>`（子组件的 `activeDiffPath` 也按字符串比对），于是传进去永远是 `undefined`——树模式与列表模式的选中行都没有高亮。改为直接传值。
  - **模式开关有个不存在的第三种模式**：`viewMode` 的类型是 tree 或 flat，第二个按钮却写 list：按下去“碰巧”能出扁平列表（走 `v-else`），但它的 `active` class 永远不亮，而 `toggleViewMode()` 又会把状态翻成 flat。统一到 flat，并把旧构建写进 localStorage 的 list 读成 flat。
  - **为什么此前一条测试都没抓到**：`GitPanel.test.ts` 把 `GitChangesView` 整个 stub 掉，分区与高亮对它根本不可见——与 3b 那条“桩替身把形状错误一起桩掉”是同一个模式。**未验证**：真 Electron 里没打开过 Git 面板复看高亮与分区效果。
  - **门禁**：desktop `npm run test` EXIT=0（73 文件 / 682 passed / 14 skipped）、`vue-tsc` **14** 条存量错、`vite build` EXIT=0。

- **存量类型债清到 0：`vue-tsc` 24 → 0（2026-09-21，第二段）**：剩下 14 条分四类，每一类都是“界面在读一个不存在的字段”或“替身与真形状不一致”，不是编译器挑剔。
  - **`graph.title` 从来不存在（2 处）**：`AgentActivityPanel.vue` 与 `TaskGraphPanel.vue` 都写 `graph.title ?? 兜底`。mint 点 `OrchestrationGraphProjection.FromSnapshot` 返回的就是 `{ tier, nodes, edges }`，而 `graph` 只有这一个生产者——也就是说那个兜底**一直**是用户看到的标题。删掉假读取；`TaskGraphPanel` 顺带把裸英文 `Plan` 换成两边都有译文的 `agent.taskPlan`（它此前完全没用 i18n）。
  - **preview 的编排快照是一份过期契约（6 处）**：`mockOrchestrationSnapshot` 的 `graph` 是带 title/时间戳的“任务图头部”，类型要的是声明图；5 个任务节点缺 `lane_key`——Core 在 `DmaeaEndpoints` 的 `LaneOfTaskSession` 里保证它永远有值（空即 main），所以这是必填；`lanes` 同样必填却整块没有，`flows`（可选）也是空的。后果是预览画廊里 Lane 头部与数据流视图**永远无内容可渲染**，与 2e 里那批假工具 id 同类。补全时词汇全部取自 mint 点：lane 状态用 `LaneStatus` 的字面量，wait 谓词用 `LaneWait.LaneTasksCompleted`，flow 的 `kind` 用投影里唯一写死的 `dispatch`。
  - **Monaco 的 detach 写错了形状（4 处）**：`setModel({ original: null, modified: null })` 在它自己的 `.d.ts` 里不合法（`IEditorModel` 的两个属性非空），而同一份声明明写着“调 `setModel(null)` 即可分离”。`useMonacoDiff.ts` 与 `PatchPreview.vue` 各一处改为 `setModel(null)`。**未验证**：vitest 里没有 Monaco，而这段代码防的正是只有实机才会抛的 “TextModel got disposed before DiffEditorWidget model got reset”——形状按接收方声明改了，效果没在浏览器里看过。
  - **两个 `vi.fn(async () => true)` 被 spread 调用（2 处）**：桩替身零参却 `confirmMock(...args)`，TS2556；把参数写进桩，运行时行为不变。
  - **顺手把两个面板挂进 i18n 契约**：`i18nParity.test.ts` 的 panelSources 加入 `AgentActivityPanel.vue`、`TaskGraphPanel.vue`。被引用的 key 若两边都缺，界面直接显示点号串；只补译文而不登记来源，洞还在。
  - **门禁**：desktop `npm run test` EXIT=0（73 文件 / 682 passed / 14 skipped）、`vue-tsc` **0 条**、`vite build` EXIT=0。**值得记一笔**：`typecheck` 不在 `npm run test` 脚本里，24 条基线时期它只是“顺手看一眼”的软门槛；现在它是 0，任何新错都会立刻显形——要不要把它升成硬门禁属于阶段 8 的架构债。

- **`read_attachment`：模型第一次能自己翻页读用户附上的文件（2026-09-21，阶段 3c 附件收尾）**：Core 侧新虚拟工具（登记清单见 `TinadecCore/AGENTS.md` 的 5b），桌面侧动的是显示与声明三处。
  - **上下文必须先给出工具要的把手**：`session_attachments` 那段此前只写「文件名 + 类型 + 字节数 + sha256 前缀」，没有附件 id——而 id 正是 `read_attachment` 唯一入参。现在每行带 `id:<guid>`，被截断的那行也不再只说「问用户要」，而是指向按 id 继续读。用例断的是这对耦合：上下文里出现的 id 必须真能被工具读到（只 assert 有 id 字符串不够）。
  - **虚拟工具镜像从注释提醒升级为契约**：`toolPresentation.test.ts` 直接 parse `CoreVirtualToolPolicy.cs` 比对 `CORE_VIRTUAL_TOOL_IDS`，并补 `read_attachment: 'read'` 的 kind（否则落进 'other'、没有图标）。这条新契约在本次只改 Core 的那一轮立刻变红，正是 #15「补了译文却没登记来源」同一族。
  - **种子包 2.5.0 → 2.6.0**：`solo_master`/`search`/`global_engineering` 声明 `read_attachment`，`meeting` 保持空工具面。按仓库规矩 version 与 digest 一起动；digest 先拿 HEAD 的旧清单复算，确认能得回原值 `2463dee…` 才信新值 `e34896a…`——防止算法写错却产出「看着对」的新数字。
  - **门禁**：Core `AttachmentApiTests` 32/32（26 既有 + 6 新）；desktop pack + presentation 39/39；四道守卫各自变异验证过。图片/音频仍读不了：这个运行时没有二进制通道，工具直接拒绝而不是回 base64，真视觉通道属独立批次。**未验证**：真机里由模型自己发起一次 `read_attachment`（工具面来自冻结清单，未在 Electron + 真 provider 下走过）。
- **纯附件消息：不打字也能把文件送进去（2026-09-21，阶段 3c 收尾）**：上传完必须配一句话才发得出去，因为 Send 只读草稿、Core 又把空 `content` 判成畸形请求。现在「正文空 + 有 ready 附件」发得出去，Core 回 `status: "message_only"`、不起 run（登记与理由在 `TinadecCore/AGENTS.md` 5c）。
  - **Send 的判定只认一个 owner**：`pendingAttachments.isSendable` 同时喂按钮的 `canSend` 与 `attachmentsForSend()`，`HomeController.sendMessage` 读同一份。uploading/failed 的芯片**不**解锁——那时还没有 `attachment_id`，放行了就是发一条空消息把文件丢掉。配对用例：ready 芯片能发、in-flight 芯片不能发（只留一条的话，删掉闸门也能骗绿）。
  - **只有芯片不算消息**：`MessageItem.vue` 的正文 `<p>` 加了 `v-if`，否则一条空正文会在气泡里留一行空白；附件条本来就按消息自带投影渲染。
  - **首条消息的标题**：`generateTitle('')` 过去回 `'New chat'`，于是「只发了 crash.log 的会话」在列表里叫 New chat。现在空正文回落到这批附件的文件名。
  - **wire 形状**：这台主机写 JSON 丢弃 null，所以「没有 run」表现为 `run_id` 键不存在（不是 `null`）。用例把这件事写死，免得以后有人按 `status` 字符串猜。

- **`src/transport/` 那层「留给未来 WS」的壳也删了，并同步纠正写着它的规则（2026-09-21，阶段 3b 第三段）**：四个文件、69 行，唯一入口是 `index.ts` 的再导出，而全仓（含 TinadecUI 卡片壳）**没有一个 importer**；`sseTransport.sse()` 收三个参数、返回一个空 `AbortController`——不发送、不接收、不报错，正是本轮一直在剪的「回答调用者以沉默」那一族。
  - **这是一次有意的规则修订，不是绕过**：`.ponytail/rules.md`（根 `AGENTS.md` 指认的 canonical 规则）原本写着「keep transport seams (`src/transport/`) for the future WS upgrade」，同一条里还说「Run streaming uses `useRunStream`」——后半句在上一刀之后已经指向一个不存在的文件。规则改为陈述现状：run 流只有一个读者 `src/lib/runStream.ts`，可注入的接缝是它的 `fetchImpl`；`src/transport/` 已删并标注 verified 2026-09-21，理由是 Core 至今不暴露任何 WebSocket 端点，所以「WS 升级」是对一份尚不存在的协议做新实现，而不是在这个接口后面换实现。`.ponytail/debt.md` 里那两行（`transport/sseTransport.ts`、`stores/workbench.ts:41`）随之删除，`useRunStream.ts` 那行的路径改指 `lib/runStream.ts`。
  - **顺手删掉的第二个死桩**：`useWorkbenchStore().decideApproval` 无条件 `throw`，注释写的是「approval transport via httpTransport」——它引用的正是本轮删掉的接缝，而**全仓没有任何调用点**（真正的裁决走 `api.decideApproval`：`HomeController` 两个入口、`CodeController`、`MarketController`；GitPanel 把决定 emit 给 CodeController，仍落到同一个函数）。删它不动任何审批门：`rules.md` 的「NEVER Remove: Approval gates」约束的是 Core 的策略与 `confirm_*` 双门，不是一个从不被调用、一调用就抛的占位——这条理由写进提交正文，便于复核。
  - **门禁**：desktop `npm run test` EXIT=0（72 文件 / 668 passed / 14 skipped）、`vue-tsc` 仍是 **24** 条存量错、`vite build` EXIT=0、`npm run ai:ponytail:validate` EXIT=0。**未验证**：`/workbench` 页没在真 Electron 里打开过，本轮只删了它的死桩。

- **一条 run 流只留一个读者，顺带救回一个静默死掉的已发布功能（2026-09-21，阶段 3b：流读取收敛）**：`api.ts` 里那份手写的 SSE 解析循环（`invokeStreamWithAdmission`）和聊天用的 `createRunStream` 读的是**同一个端点** `/api/v1/runs/{id}/stream`，但各读各的。收敛成一条：durable 读者从 `composables/useRunStream.ts` 搬到 `src/lib/runStream.ts`（`useRunStream()` 那个 Vue 包装器**零消费者、零用例**，删），api.ts 不再自己解帧，只做"准入 + 交给 durable 读者"。
  - **删掉的那份读者具体丢什么，是量出来的不是推的**：① 没有 CRLF 归一化（durable 读者显式 `
`→`
`）；② 响应在最后一帧后直接结束时，那一帧被丢在缓冲区里（旧循环只认 `\n\n` 分隔，从不 flush 尾巴）；③ 非终态断流不重连、不带 `Last-Event-ID`。A/B：把 api.ts 换回 HEAD（收敛前）版本跑新增的 13 例 → 3 例红，红因正好落在 ②（两例）与下面的 delta 丢失（一例）。
  - **真正被修掉的用户可见缺陷**：`ModelStreamChunkDto` 把 `delta` 声明在**顶层**，而 Core 的帧把文本放在与 `kind` 同级、经 `parseRunSseBlock` 后落在 `chunk.payload` 里。旧 compat 读者手工拼 chunk 时**没有带上 delta**，于是 Git 面板那两个 AI 功能（提交信息 / 变更分析）`chunk.delta` 永远是 `undefined`：整条回复流完了，界面什么也不显示，也不会报错——`done` 分支里 `if (text)` 直接把空文本咽了。这条链路此前**一条测试都没有**（`GitPanel.test.ts` 把 `api.invokeStream` 桩成 `new AbortController()`，所以桩替身把形状错误一起桩掉了）。现在由 `modelStreamChunkOf` 做一次提升，`api.test.ts` 里那例把话钉住；把 lift 改成 `delta: null` → 恰 1 例红。
  - **`runStreamDelta` 也只有一个 owner 了**：控制器原来有一份同名私有函数（`streamDelta`），逻辑一字不差。两处读帧形状迟早分叉，所以它搬进 `lib/runStream.ts` 并导出，控制器与 compat 提升都用它。**这也是把读者放进 `lib/` 的理由**：`api.ts`（传输层）不该去 `composables/` 里取一个协议实现，而 `lib/ → composables/` 此前一个先例都没有。
  - **有意的行为变化（写清楚）**：① compat 路径 `autoReconnect: false` —— 一次提交信息生成不该在取消后变成退避重试循环；② 代价是非终态断流现在会走 `onError`（旧代码静默返回），这与 durable 侧既有语义一致；③ 准入回执的 `stream_cursor` 变成 `after_seq`，不再从 0 重放；④ 那句 `api as unknown as { invokeStreamWithAdmission: … }` 的自类型强转消失，两个入口各自调用同一个模块级函数。
  - **门禁**：`npm run test` EXIT=0（71 文件 / 1 跳过，api.test.ts 13 例，其中 5 例是本轮新增）、typecheck 仍是 **24** 条存量错（`api.ts`/`lib/runStream.ts`/`HomeController.ts` 零错）、`vite build` EXIT=0。**量到但本轮没动**：`transport/sseTransport.ts` 的 `sse()` 是返回空 `AbortController` 的占位，它的注释已随本轮改指 `lib/runStream.ts`，但这个 seam 本身仍是"回答调用者以沉默"的空壳（只被 `transport/index.ts` 再导出、零消费者）；`debug/composables/useDebugWebSocket.ts` 仍连 Core 不存在的路由且无条件 3s 重连——两者与刚被删掉的"第二个读者"同族，是 3b 的下一刀。**未验证**：两个 AI 面板没有在真 Electron + 真 provider 下跑过；本轮证据止于"传输层把文本交到消费者手上"这层断言。

- **命令面板与全局键位（2026-09-21，阶段 3c 第二段）**：上一段那张表终于有了第二个消费者。
  - **`lib/keybindings.ts` 是窗口内唯一的按键 owner**：`registerKeybinding(combo, run, when?)` 注册即占用同一 combo，第二次注册**抛错**而不是静默覆盖——"一个键位两个主人"正是这层要消灭的东西，而注册表小到可以直接修。`installGlobalKeybindings()` 按 owner 计数，最后一个走才摘掉 document 监听（与 `sessionEventBus` 同形）。`Mod` = mac 上的 ⌘ / 其他平台的 Ctrl，匹配时**逐个比对 shift/alt，未被点名的修饰键必须没按下**：否则 Mod+K 会顺手吃掉 Mod+Shift+K。此前散落四处的 `document.addEventListener('keydown')` 仍然各自守着自己的表面（选区菜单、行右键、通知岛、终端快捷键），它们只在自身打开时动作，所以面板不需要去抢；面板自己的 Escape 也留在元素上——那是焦点局部的事，在这层伪造一个焦点栈才是更大的谎。
  - **面板是原生 `<dialog>` + `showModal()`**，沿用 `NotificationDetailDialog` 的现网做法：焦点陷阱、top-layer（不再参与 z-index 军备）、Escape、关闭后把光标还给原来的元素，全部是浏览器给的。第一版我用手写 backdrop + Teleport 自己实现，其中 Escape 与"归还焦点"两段就是在这里被删掉的——自己写这部分是键盘陷阱的制造方式，不是工程。
  - **`composables/useCommandPalette.ts` 只有 `App.vue` 能装**：面板状态是窗口级的（在设置页、市场页、分离面板里都要能按出来），所以它不能挂在任何页面组件上。这条约束由 `useCommandPalette.test.ts` 里一例**扫全 `src/**`** 的结构断言把守：`installPaletteKeybinding(` 与 `<CommandPalette` 各自只能出现在 `../App.vue`。变异验证：把 App 里的安装调用换成 `undefined` → 该例报 `expected [] to deeply equal [ '../App.vue' ]`。宠物窗口刻意不装（一个桌宠没有命令可跑）。
  - **四条断言都有牙（逐个变异，改完即恢复）**：`canStop: () => true` → 2 例红（面板被测成"列出一切"而不是"列出此刻能做的"）；`run()` 里把 `implicitArgument(...)` 换成 `""` → 1 例红，红在 `expected to be called with [ '排在后面这条' ] / Received [ '' ]`（行必须带回它宣称要发的文本，否则是一次看起来成功的空发送）；`element.showModal()` 换成 `setAttribute('open','')` → 1 例红。第三条不是白捡的：happy-dom 下两种写法都会让 `dialog.open === true`，所以那一例改为**监视接收方自己的方法**（把 `showModal` 换成 spy），只断言属性的话，"真模态"和"一个被刷成打开样子的 div"根本分不开。
  - **门禁**：desktop `npm run test` EXIT=0（71 文件通过 / 1 跳过，较上一轮多 3 个文件），`vue-tsc` 仍是 **24** 条存量错误且**新增文件零错误**，`vite build` EXIT=0。**已知缺口（诚实）**：~~面板目前只有键盘入口~~（2026-09-22 已补，见下条）；`homeController.rightRailCollapsed` 无消费者（上一段量到），故面板也没有视图类开关；Escape 语义采用原生（直接关闭），不清空已输入的查询。**未验证**：真 Electron 里没打开过面板（焦点归还、::backdrop 模糊、列表滚动到选中行都只是断言级证据）；`Mod+K` 在 xterm 终端获得焦点时是否被终端吞掉没有实测。

- **命令面板的鼠标入口落在 `AppHeader.vue`（2026-09-22，阶段 9 第一项）**：上一段那条"没有可挂搜索按钮的现成活表面"的结论**是错的**，错在只查了 `AppSidebar`。真正的活 chrome 是 `src/components/AppHeader.vue`：`apps/TinadecUI/src/components/UieShell.vue:3` 以 `@/components/AppHeader.vue` 引入它并在 `.shell` 里渲染（HomePage/ChatroomPage 经 `<UieShell/>` 得到它），CodePage/LibraryPage/MarketPage 又各自直接挂载——两处 `@` 都指 `apps/desktop/src`（`vite.config.ts:12`），所以改一个组件就能覆盖所有带顶栏的页面，**不需要跨包改布局**（覆盖面到底有多大见下一条：不是全部页面）。
  - 按钮只 `openPalette()`，不装第二个键位、不渲第二个面板：`aria-label` 取 `palette.title`，`aria-keyshortcuts`/`title` 取 `useCommandPalette().comboLabel`（即 `formatCombo(PALETTE_COMBO)`），因此**提示的键位与真实绑定同源**，不会在 mac 上写 Ctrl。`aria-haspopup="dialog"` + `:aria-expanded="open"`；面板是 `showModal()` 模态，打开期间按钮本就点不到，所以入口用 open 而不是 toggle（写成 toggle 是死代码）。
  - **覆盖范围是量出来的，不是推断的**：有顶栏的 5 页得到入口（Home/Chatroom 经 `UieShell`，Code/Library/Market 直接挂 `AppHeader`）；`WorkbenchPage.vue` 与 `SettingsPage.vue` 模板里既没有 `AppHeader` 也没有 `UieShell`（逐文件 grep，2026-09-22），所以这两页当时**只有键盘入口**（同日下一段把它们连同 Governance / Snapshots 一起补上了）——面板本身照旧能用（owner 仍是 `App.vue`）。补齐这两页的鼠标入口是阶段 9 的下一项，别把它算作已完成。
  - `AppHeader.test.ts` 两例：一条比对 `formatCombo(PALETTE_COMBO)` 的**返回值**而不是字面量（写死 `Ctrl+K` 在这台机器上会绿、在 mac 上撒谎），一条点击后断言 `paletteIsOpen()` 与 `aria-expanded`，并断言头里 `findAll('dialog')` 为 0（面板仍只有一个 owner）。
  - **顺带修的门禁债**：`vite.config.ts` 的 vitest 段显式 `testTimeout: 20_000`。此前一直用 5s 默认值，而这条套件里有真做全树扫描与 happy-dom 挂载的用例，代价随机器负载变化——同一轮里 `toolPresentation`（源码扫描）与 `ChatroomPanel`（面板挂载）在 dotnet 解决方案门禁并行时双双超时，而这两例在空闲机上一直是绿的；被扫掉的第三、四个受害者是 `SettingsPage.agentPack` 与 `AppearanceSection`。**未验证**：真 Electron 下这个按钮的观感与 -webkit-app-region 拖拽区共存时的可点性仍未实测（`.topbar button` 已有 `no-drag`，是断言级证据之外的推断）。

- **入口抽成一份组件，四个面各自挂上（2026-09-22，阶段 9 第二项）**：上一段留下的"Workbench/Settings 只有键盘入口"补上了，并且把入口做成了可复用的单一 owner：`components/CommandPaletteButton.vue`。
  - **为什么抽组件而不是复制四遍**：Workbench（`.workbench-controls`）、Settings（`.settings-window-controls`）、Governance（`.approval-board__header-actions`）、Snapshots（标题行 `gap-2`）各有顶栏且都没有 `AppHeader`。四处各写一份 `<UiButton>`，属性就会各自漂移，其中 `aria-keyshortcuts` 漂移最贵——它向读屏软件承诺一个平台并不存在的键位。组件内部仍只是 `openPalette()`：键位归 `lib/keybindings.ts`，面板元素归 `App.vue`，这两条结构断言本轮一例没削。`AppHeader.vue` 改为引用它，其自带测试也随之改成"头里确实渲染了共享组件、且只有一份"，属性与点击的断言搬到 `CommandPaletteButton.test.ts`（owner 自己守自己的契约）。
  - **覆盖面是量出来的**：`useCommandPalette.test.ts` 新增一例扫 `src/pages/*.vue`，把"既无 `CommandPaletteButton`、也无 `AppHeader`、也无 `UieShell`"的页面**精确等值**比对：`DebugStudioPage`（开发者窗口）、`DesktopPetPage`（桌宠没有命令可跑）、`DetachedPanelPage`（整条标题栏只有一个"归位"按钮）、`RecoveryCheckPage`（**刻意留空**：那是一页是/否裁决，为一个图标去新开动作行属于改版式，没有真机走查不做）。变异验证：从 `SnapshotsPage.vue` 摘掉挂载与 import → 该例报 `+ "SnapshotsPage.vue"`，不是空红。
  - **修掉一条我自己写的假阳性**：那条既有扫描用 `source.includes('<CommandPalette')` 找"谁渲染了面板"，而 `<CommandPaletteButton />` 恰好带这个前缀——新组件一挂，五个文件立刻被判成第二个面板 owner。改成 `/<CommandPalette(?![A-Za-z])/`，把匹配钉在标签名结束处。**给组件改名来绕开子串测试是更坏的选择**：那等于让这条断言从此说不出话。
  - **门禁**：desktop `npm run test` EXIT=0（75 文件通过 / 1 跳过，较上一轮 +1 文件），`npm run typecheck -w @tinadec/desktop` EXIT=0。**未验证**：真 Electron 下这四个新位置的观感——按钮沿用 `.window-btn`（40×32）与 `no-drag`，但它落在按钮组里（Workbench 的三个动作按钮、Snapshots 的下拉框旁）会不会偏大/错位，happy-dom 只证明属性与点击，不证明像素。

- **记忆评审面第一次可用（2026-09-22，阶段 6 第二段）**：`pages/MemoryPage.vue` + 路由 `/memory` + 面板命令 `view.goMemory`。**在此之前桌面从未调用过任何记忆端点**——`api.ts` 里连候选类型都没有，而 Core 的策展人一直在往队列里写。四条不是顺手写的规矩：① 卡片必须给出 `evidence`/`applicability`/`expiry_condition`（Core 这次把这三项从**早已存在**的 proposal blob 里投影出来），只看一句"该用 pnpm"而看不到依据与失效条件，评审就等于对句子本身表态；② **两个架子永不合并**——候选不是记忆，晋升的条目已经在影响模型，混在一起等于让一次点击代表两件事；③ **空态与失败态必须分开**：`loadError` 时不再渲染"没有等待你的条目"，这条是被自己的断言逼出来的（先写断言，组件当时确实在撒谎；变异验证：去掉 `&& !loadError` 该例立即红）；④ **撤销不收集理由**——`MemoryItemRecord` 没有 reason 列，`RevokeAsync(itemId, reason)` 收下就丢，收一个会被扔掉的答案等于教用户输入噪音（缺口登记在 `TinadecCore/AGENTS.md`）。
  - **DTO 逐条对铸造点**（#21 这类缺陷的第三次，这次在动手前就对了一遍）：`MemoryCandidateDto`/`MemoryItemDto` 按 `MemoryReviewEndpoints` 的两个匿名投影手写；`MemoryRevocationDto` **故意不等于** `MemoryItemDto`——revoke 响应里没有 `content`/`created_at`，套上列表类型就是允许调用方去读一个端点从不发的字段。Core 边界**不写 null 键**（Core 测试 `AnAbsentConstraintIsAbsentRatherThanEmpty` 实测：老候选读回来连 `applicability` 这个键都没有），所以可选字段一律 `?: string | null`，页面按真值判断而不是按键存在。
  - **分页要说"可能有更多"**：两个列表各带 `limit=100`，正好 100 行时明写"只显示前 100 条"，不把一页当成全表；置信度不是数字时渲染"未打分"而不是 "NaN%"（这条也是 `OrchestrationTab` 那次的教训）。
  - **门禁**：desktop `npm run test` EXIT=0（**78 文件通过 / 1 跳过、716 例通过 / 14 跳过**，较上一轮 +1 文件 +11 例，即 `MemoryPage.test.ts`），`npm run typecheck -w @tinadec/desktop` EXIT=0，`vite build` EXIT=0。**`check:drift` 真的会看见网关新路由**：本批 `src/generated/schema.d.ts` 重新生成 +29 行（两条 memory-items 路径），必须与网关快照同批提交，否则该门禁红——先前"drift 看不见代理路由"的说法是错的，已按实测纠正。`CommandPalette.test.ts` 那行 `toHaveLength(10)` 是**刻意留的 canary**：面板首屏就是这些命令，加一个页面就必须有人来看一眼。
  - **本轮补上的那条链**：`FullDuplexEndpointTests.InvokeStream_SendsPromotedMemoryAndKeepsAnUnreviewedCandidateOutOfThePrompt` 是仓库里第一次断言"评审说 yes 之后，模型真的被告知了"——晋升记忆以 `[reviewed_memory]` 进入 prompt、未评审的候选绝不进入、`context_packs.sources` 里出现 `reviewed_memory`。此前这一段只能靠读代码相信。**仍未验证**：真 Electron 里这张面的观感与密集度没有像素级复看；检索排序仍是关键词命中比例（向量化路由未配置），所以"换个说法的同一条事实"召不回来。

- **run 的证据清单进了界面，顺手修掉一桌面端自己造的字段（2026-09-22，阶段 6 第一段）**：Core 现在把 `context.packed` 的来源名写进 payload（见 `TinadecCore/AGENTS.md` RUN EVIDENCE TRACE），`Context Packs` 一行因此第一次说出"这一轮被告知了 `workspace_instructions` / `workspace_skills` / `session_history` …"。**动手前 `api.ts` 里的 `ContextPackDto` 与真实投影完全不符**：声明了 `session_id`、`created_by_agent_id`、`summary`、`compression_ratio`、`evidence_map` 五个 Core 从不发的字段，而 `run_id`/`evidence_count`/`sources` 一个都没声明——于是模板里 `Math.round(pack.compression_ratio * 100)` 每行渲染成 **"NaN%"**，标题行是空的。这是任务 #21（`SessionInteractionDto` 声明了 Core 从不发送的字段）的**第二次发生**，同一手法：只写类型不比对铸造点。**三条留下规矩**：① DTO 字段必须逐条对得上铸造侧（这次是 `DmaeaEndpoints.cs` 的 `context_packs` 匿名对象），发不出的字段一律删，不为了"界面想显示"编一个（`summary` 就是这样删的——`EventEnvelope` 根本没有该字段，行标签改由 `lane_key` 推导）；② `debug/preview/mockData.ts` 必须用同一个形状，**mock 用假形状会把组件教会一个服务器永远给不出的契约**，本轮 mock 就是那个 NaN 从未在开发机上暴露的原因；③ `sources: []` 要说"未记录"而不是"空"——历史事件永久缺这个键，把它显示成空列表等于撒谎。**验证**：`OrchestrationTab.test.ts` 4 例（来源逐个出现、无 NaN、旧事件走"未记录"文案、lane 与 planner 标签分别推导）；变异验证：把 `Math.round(compression_ratio*100)` 那一行原样加回去 → 该例红于实际文本含 `NaN%`（不是靠元素不存在蒙过）。**门禁**：desktop `npm run test` EXIT=0（77 文件通过 / 1 跳过，较上一轮 +1 文件），`npm run typecheck -w @tinadec/desktop` EXIT=0。**未验证**：真 Electron 里这行 chip 的观感（密集程度、换行）没有像素级复看；每条来源各自的 token 花费 Core 未发，界面目前只能看出"少了哪条"，看不出"谁把它挤掉的"。**随行一条要知道的副作用**：`c0402e8` 里 `mockData.ts` 显示 1955/1946 行——那是**一次性行尾归一**，该文件的旧 blob 以 CRLF 入库，而 `.gitattributes` 的 `text=auto` 在 `git add` 时把它清成 LF，于是整文件被记为改动；真实内容差是 36/27 行（`git diff --ignore-cr-at-eol` 里仍可看到成对出现的纯行尾噪声）。**判断"我改了几行"要看的不是 `--numstat` 的第一反应，而是这条命令 + 内容 grep**；以后碰到这种文件，宁可在这段说清楚，别让人对着两千行 diff 猜。

- **快照的逐文件评审与单文件撤销（2026-09-22，阶段 4）**：Core 侧把一直存在、却从没交出去的工作区快照逐文件清单开了三条口（`GET /api/v1/workspace-snapshots/{id}/files`、`GET .../files/diff?path=`、`POST .../files/restore`，细则见 `TinadecCore/AGENTS.md` 的 PER-FILE SNAPSHOT REVIEW 段），面落在 `pages/SnapshotsPage.vue`：每张快照卡多一个 Review files 开关，行内 Compare 把两侧正文交给**现成的** Monaco `components/git/DiffViewer.vue`（不另写 diff 引擎），Undo 只撤这一条。**四条不能让位给方便**：① `unchanged` 行一律不列——一张四十行的评审卡最后的下场就是没人读；② `restorable:false`（快照只记了哈希没记正文）只显示原因、**不画按钮**，客户端自己按大小猜会造出一个点了什么也不会发生的控件；③ 撤销必须带回**该行显示的那个工作者刚看到的哈希**，`expected_sha256` 缺失会被 Core 判成"没人评审过"（400），把字段"优化"成可空就等于开了盲覆盖的口子；④ 被删掉的行没有当前哈希，"当时这里没有文件"用**空串**表达，不是省略字段。状态色（`--accent-danger`/`--accent-warning`）只强化 `data-status` 已经写出来的词，含义不单靠颜色。**验证**：`SnapshotsPage.test.ts` 4 例（滤掉 unchanged、撤销参数逐条比对、不可撤行没有按钮而有原因、Compare 请求带路径且把两侧正文交到 DiffViewer 手上）——测试里 DiffViewer 是 stub，这既是绕开 happy-dom 加载不了 Monaco，也正是为了断言**传给它的那五个 prop**。**未验证**：这一页没在真 Electron 里对着真工作区走过一遍，卡片本已密集，塞进评审区后的观感没有像素级结论。**契约覆盖是半边**：外部 OpenAPI 一变，`npm run check:drift` 就要求重新生成 `src/generated/schema.d.ts`（本轮它确实红了，三条路径/请求体因此进了契约，+73 行随本批一起提交）；但这三条路由在快照里**没有响应体 schema**（`responses.200.content: never`），响应字段漂移看不见，`api.ts` 里这三个响应类型仍是手写的——Core 测试与本页调用参数断言是夹住字段的两头。

- **命令只有一份表，斜杠菜单与命令面板都从它读（2026-09-21，阶段 3c 第一段：命令注册表收敛）**：动手做面板前先量了一遍现状——斜杠命令的**定义**在 `lib/composerCommands.ts`，**处理**却在 `ComposerBar.vue` 的 `switch` 里。面板如果自带第二份 switch，就是"铅笔按钮接了个桩"那类缺陷的第二次发生：多一个 surface，就多一批只被显示、从未被实现的按钮。
  - **`src/lib/appCommands.ts` 是唯一 owner**：条目 = 数据（`id`/`group`/`labelKey`/`slash`/`needsArgument`/`keywordKeys`/`route`）+ `isAvailable(host)` + `run(host, argument)`。`composerCommands.ts` 整个删除，它 7 条用例原样搬进 `appCommands.test.ts`（解析/前缀过滤的行为一个字没改）。这张表**不 import 控制器、不 import router、不 import i18n**：副作用一律经过 `CommandHost`，标签只给 key——这正是它能被纯逻辑测 22 例、以及 `src/lib` 不再长出第二份应用状态 owner 的原因。
  - **两个 surface 的差别只在 host**：ComposerBar 的 host 持有 textarea 高度、`modeVersionId` / `meetingModelOverride` 和 `emit('stop')` 链路（stop 刻意仍走 emit 而不是就地调 `homeController.stopRun()`：ChatCard→controller 是现网路径，只缩短两个调用者中的一条正是两边漂移的起点）。面板的 host 会带 router。
  - **`go()` builder 不是嫌打字多**：9 条导航条目若各写内联闭包，路由名就只活在函数体里，`command.route` 字段无从枚举——而测试"没有一条面板命令指向 `router.ts` 里不存在的路由"靠的就是这个字段（它 `?raw` 读 `router.ts` 比对 `name: 'xxx'`，与 settings CSS 契约、附件字节上限同一手法：断言读的是接收方自己的属性）。
  - **一条差点写错的可用性规则**：`queue/parallel` 一度被我加上"要有会话"。实测 `handleSend`（`HomeController.ts:394`）在无会话时会**自己创建**会话，那条过滤会把 hero 页上今天走得通的路径砍掉。规则因此改成只看"有没有文本"，并且留了一例把理由写死：`sends without a session, because the controller creates one`。
  - **A/B 证明委托有牙**：把 `run.queue` 的 dispatch 改成 `'parallel'` → 2 例红，其中一例是**组件级**用例 `sends the payload of /queue as a one-shot queued dispatch`；ComposerBar 确实执行的是表里的函数体，而不是自己那份残留逻辑。改前/改后各跑一次，已恢复。
  - **门禁**：`npm run test` EXIT=0（vitest 之外还有 electron `node --test` 24 例），`vite build` EXIT=0，typecheck 回到 **24** 基线（中途 27，原因是 `AppCommand.route` 先被测试用掉、字段还没补）。**顺带量到**：`homeController.rightRailCollapsed` 导出后没有任何在用的消费者（只有 `debug/preview/PagePreview.vue` 自己的同名局部 ref），所以面板刻意不含"折叠右栏"这类命令。**未做**：面板 UI 与全局键位在 3c 第二段，`palette.*` 目前只有行标签和搜索词，标题/空态/页脚提示键随组件一起加。**未验证**：真 Electron 下斜杠菜单四项的观感没有复看（本轮纯委托改写，渲染路径未变）。

- **每窗口一条 session 事件流（2026-09-21，阶段 3a，流订阅收敛）**：基线不是"也许重复"而是数出来的三个调用点：主窗口两条、每个分离面板两条 `/api/v1/events`——`HomeController` 的事件订阅一条，`useAgentActivity.connect` 自己再开一条（`DetachedPanelPage` 又一条），每个事件因此被解析两遍、服务端也为同一窗口多养一条长连接。扇出形状本来就存在（`TerminalCallBlock` 走 `homeController.onEvent`，注释还写着"控制器拥有那条 SSE"），只是没人执行。
  - **新 owner = `src/lib/sessionEventBus.ts`**：`followSession(id)` 决定这一窗口跟随哪个会话（null = 未过滤流，与控制器原行为一致），`subscribeToSessionEvents(handler, { scope })` 才是消费者该用的东西。**`api.connectEvents` 在渲染层因此只剩一个调用点**，而这条保证不是靠自觉：`sessionEventBus.test.ts` 有一例**扫全 `src/**`**，任何别的文件出现 `connectEvents(` 就红（变异验证：临时放一个调用它的文件 → 报出 `lib\__scratch_owner.ts`；文件名会印在断言里，不是一句空红）。`generated/` 与 `debug/preview/mockApi.ts`（它是 api 对象的替身）在白名单里。
  - **scope 只丢"点名了别的会话"的帧，不丢没有 session 字段的帧**——这不是保守，是量出来的：Core 的 `/api/v1/events` 允许不带 session 过滤（`StorageEndpoints.cs:291`：`sessionId ?? session_id` 可为 null），持久日志里确实有 session-less 行；若把"未知"判成"不是我的"，一次字段缺失就能让整块活动面板 blackout。反过来，`scope.value` 为 null（会话已关）时该订阅者**一条都不收**，这正是它以前"不连"的语义，不能因为换了共享流就变成渲染别人的残留。
  - **顺带修掉一个只有共享才暴露的缺陷**：`useAgentActivity` 用 `onUnmounted` 清理订阅，而那是**组件**钩子——在 `effectScope` 里（分离面板、测试夹具）它**从不触发**。共享之前这只漏一个没人引用的 EventSource，共享之后就是一个死视图永久扣住窗口唯一的连接。这不是推演：切到总线的同一轮里，该套件 8 条既有用例同时红在 `mount()` 的"必须已订阅"守卫上，原因正是前一个范围没退订、旧连接还在，新范围拿不到自己的实例。换成 `onScopeDispose` 后 10/10。
  - **A/B 而不是断言**：把 `useAgentActivity.ts` 换回 HEAD（收敛前）版本跑新增那条断言 → `to have a length of 1 but got 2` 红；换回本轮版本 → 10/10。该套件的价值也保住了：它用 `FakeEventSource` 走**真实命名帧 + 真实 `api.connectEvents`**，所以"事件名漂移让工具结果永久不可见"那类缺陷仍会被它抓住——本轮刻意没有把它改成桩总线。
  - **门禁**：`npm run test` 68 文件通过 / 1 跳过（EXIT=0），`vite build` EXIT=0，typecheck 回到 **24** 基线（中途 28，是测试桩 `stubConnection` 签名不匹配，非产品问题）。**未做**：`api.invokeStreamWithAdmission` 仍是第二套手写 run-stream reader（无游标续传、无 `run_id+seq` 去重、无退避重连），它与 `createRunStream` 的合并是 3b；`useDebugWebSocket` 仍指向 Core 里不存在的路由且无条件 3s 重连；`ChatroomPanel` 5s 与 `stores/userAction` 2s 轮询未动（不是 SSE，是否收敛属阶段 6/7）。**未验证**：真 Electron 下"每窗口一条流"没有开 DevTools Network 走过；断线重连只剩 `EventSource` 自身语义（本轮没加客户端重连，控制器以前也没有）。

- **消息携带附件，`MessageDto` 交出所有权（2026-09-21，阶段 2g）**：把"上传过的文件"变成"这条消息带的文件"。
  - **`src/api.ts` 里那份手写 `MessageDto` 删了，改为再导出 `@/generated/client` 的契约类型**。理由不是整洁：`attachments` 现在由契约拥有，两份同形声明正是"字段到了渲染层却看不见"的下一次事故（该文件第 24 行本来就写着 generated 是 canonical、api.ts 是 compat alias，只是没人执行）。副作用全部量过：切换后 `npm run typecheck` 从 24 涨到 **38**（`mockData.ts` 8 条消息字面量 + `ConversationPreview.vue` 1 条 + 测试里的字面量），逐处补 `run_id`/`attachments` 后**回到 24**——#15 的存量一条没少，也没有新增；`mockData.ts` 现在有一条消息真带附件投影，预览面板展示的是构建给得出的东西。
  - **先取后清**：`attachmentsForSend()` 只读不动，`settleSentAttachments(bundle)` 在 Core 应答之后才摘芯片；失败一条都不清（清掉就等于把用户无法重发的上传弄丢，而那些行还没人引用）。清空**绝不删行**——Core 刚把它们绑到消息上，删了就打断历史；这一条钉在"清空后 `deleteAttachment` 一次都没被调用"的断言上，变异验证：在 settle 里加一次 `deleteQuietly` 即红。上传中的芯片不在 bundle 里，因此也不被清（同一例钉住两半）。
  - **转向（`dispatch_mode: insert`）本地先拒**：Core 也拒（`attachment_dispatch_unsupported`，它不追加消息，绑了就是永久孤儿行），但让请求先出门就等于让用户看着一条消息发出去而附件静默消失；拒绝走 `run()` 的 notify，和既有的 `插入模式需选择目标 run` 同一条通道（实测：删掉这条守卫，只有"转向不能带文件"那例红）。
  - **`MessageItem.vue` 只渲染消息自带的投影**：链接一律 `api.attachmentContentUrl(id)`，测试断言对的就是这个函数本身的输出而不是手写路径字符串（路径挪过时，字面量断言会假装通过）；`image/svg+xml` **不做 `<img>`**（Core 对 svg 是下载语义，内联就是让渲染器执行用户的文件），其余类型是带文件名与体积的下载。`?? []` 只为本地构造的乐观行与夹具存在，不是给线上兜底。
  - **手工投影与契约对质**：`attachmentsForSend()` 造出的键集由测试读 `TinadecGateway/tests/__snapshots__/openapi.external.json` 的 `MessageAttachmentSummary.properties` **逐键相等**（与 2f 对质 Core 上限同一手法）；网关快照测试同时钉住嵌套投影里没有 `session_id`/`message_id`、以及任何一层都不出现 `content_reference`。
  - **门禁**：desktop `npm run test`（vitest + electron node 测试）EXIT=0、67 文件通过 1 跳过；`vite build` EXIT=0；Gateway `bun test` 51/51；`generate:client` 重跑无 diff（`check:drift` 要生成物与源码同批入 index 才绿）。本轮自己也被骗一次：先跑了并不存在的 `npm run test:unit`，退出码 1 来自 npm 而不是测试——脚本名要读 `package.json`，不能猜。
  - **明确未做**：只带文件、正文为空的消息还是被拒（Core 端点与 `ProjectSessionStore.cs:381` 两处），所以发文件必须写点什么；模型仍然**打不开**非文本附件——图片/音频只有清单和哈希可见，缺的是 `read_attachment` Core 虚拟工具。**未验证**：真 Electron 下消息气泡里附件条与缩略图的观感；乐观行（本地投影）与重读后（服务端投影）两条渲染路径没在同一台机器上走过。
- **输入框附件选择器与附件条（2026-09-21，阶段 2f）**：接上 2c 那条"两个 emit 全仓无人监听"的死线。**取字节走渲染进程原生 `<input type="file" multiple>`，不走特权 IPC**——实测 `electron/preload.cjs:46,49` 只有 `selectBackgroundFile(type)` 与 `readImageAsDataUrl(source)`，那是壁纸语义（按路径、只认图片），要复用就得为"读任意文件"新开一条能把绝对路径带进渲染层的通道；字节改为经 Gateway 裸 body 上传，路径一个字都不出渲染层。`add-image`/`add-file` 两个事件声明已删（留着就是下一轮再被人"接上"一次假线）。**单一 owner = `src/lib/pendingAttachments.ts`**，不放 HomeController：上传、孤儿回收、上限判定是要独立测的逻辑，且这样 `ComposerBar.test.ts` 那份只桩了 `listAgentModeTopologies`/`listDirectory` 的 `@/api` 假面不必长出新路由。状态是**模块级**（跨路由卸载保活，回到同一会话芯片还在），因此释放条件不能只靠组件内的 watch——`reconcileSession(sessionId)` 比较"这条切片属于哪个会话"，在 `attachFiles` 开头和 ComposerBar 的 `immediate` watch 两处调用：会话可能在**没有任何东西挂载时**改掉。三条不是顺手写的规则：① **上限本地先判**——`File.size` 在字节上线前就是已知的，32 MB 不该传完才失败；`MAX_ATTACHMENT_BYTES` 由测试直接读 `TinadecCore/AspNetCore/Endpoints/AttachmentEndpoints.cs` 源码、把 `MaxAttachmentBytes` 的表达式**算出来**再对质（不是字符串比对），漂了就红；变异验证：把常量改成 16 MB 后该例与两处标签例立刻红。② **孤儿行必须回收**——芯片被去掉（点 ×、切会话）而上传随后落地，那一行没人引用：`uploadOne` 见 clientId 不在就删，`reconcileSession` 批量删已落地行。删除失败**故意吞**（不可见行不该变成发送错误），代价写在函数注释里而不是藏在 catch 里：清理失败的行会作为不可见残留留在磁盘。③ **状态不能只靠动画**——uploading 除 spinner（`aria-hidden="true"`）还要出文字，整条 `role="list"` + `aria-live="polite"`，移除按钮的可访问名带文件名；ready 什么都不写，"没有状态行"本身就是确认。hero 面（无会话）两个入口是 `disabled` + `title=composer.attachNeedsSession`，**不是**先把字节扣在内存里等会话出现：Core 的归属在写入时就锁死 `(tenant, workspace, session)`，延迟上传要养着字节跨会话切换。i18n 八个 `composer.*` 键双语同提交（ComposerBar.vue 已在 `i18nParity` 的 panelSources 里，缺键即红）。**测试**：`pendingAttachments.test.ts` 25 例（2g 后 31 例）+ `ComposerBar.test.ts` 8 例；本轮被自己的时序骗过一次——`attachFiles` 同步落芯片、但 api 调用要等 `await file.arrayBuffer()`，所以"拿到 mock 的 resolve 句柄"必须先过一拍（`tick()`），第一轮 4 条红全是这个，不是产品缺陷。**同期查明（记进门禁清单）**：`npm run typecheck`（vue-tsc）在本分支**开局就有 24 条错误**，全在附件切片未触碰的文件（`GitChangesView.vue` 10、`mockData.ts` 6、`useMonacoDiff.ts` 2、`PatchPreview.vue` 2，另有 4 条单点），其中 `GitChangesView.vue` 有 4 条是**模板引用了不存在的属性/联合值**——那不是类型噪音，运行时就是 undefined 渲染；本仓库当前可复现的绿色门禁是 `vitest` + `vite build`，typecheck 待收（#15）。**已被 2g 接上**：绑定的 Core 半（白名单、上限、`insert` 拒收、上下文注入）与桌面半（发送带 `attachment_ids` + 消息气泡渲染附件条）都在下一段；本阶段落地的行当时确实全部 `message_id = null`，那句记录的是 2f 结束时的状态。**未验证**：真 Electron 里操作系统文件对话框的实际观感；Core 侧 413 流式截断（见 `TinadecCore/AGENTS.md` 的 SESSION ATTACHMENTS 段）。

Terminal renderer crash（2026-09-18）：启动时出现的 `Cannot read properties of undefined (reading 'dimensions')` 已由真实 DevTools 堆栈定位到 `@xterm/xterm` 的 `RenderService.dimensions -> _renderer.value.dimensions`。根因是 `terminal` Uie card 错误声明为非 singleton，旧持久布局可同时挂载多个 `TerminalCard`，它们共享同一模块级 terminal id/状态；一个 view 的 detach/dispose 会释放另一个 view 正在使用的 xterm renderer，异步渲染随后访问已释放 renderer。修复：terminal descriptor 设为 singleton（多 shell 本来就在 `TerminalPanel` 内部 tab 化），layout repair 自动去掉旧快照中的重复 terminal card；`TerminalView` unmount 传入自己持有的 terminal，`detachTerminal` 只允许当前 attachment owner 释放；xterm `Terminal` / `FitAddon` 存入响应式状态前用 `markRaw`，不得让 Vue Proxy 进入 xterm 私有对象图。回归：TinadecUI repair/reducer 31/31、Desktop terminal/sidebar 18/18；真实 Electron 往返聊天室→设置→首页后界面正常、右栏仅一个“集成终端”，DevTools 无新的 `dimensions` / renderer uncaught。

The General settings page owns the Desktop Gateway endpoint. The effective URL is resolved in this order: `TINADEC_GATEWAY_URL`, Electron `userData/settings.json`, then `http://127.0.0.1:48730`. Environment-managed values are read-only in the UI; persisted changes take effect after Desktop restarts.

## STRUCTURE
```
apps/desktop/
├── electron/          # Electron main, preload, Debug Studio window, panel window manager, terminal manager, layout store
├── scripts/dev.mjs    # Vite then Electron launcher
└── src/
    ├── pages/         # hash-router route pages
    ├── controllers/   # per-page domain controllers (Home/Code/Market) — module singletons
    ├── vapor/         # Vapor rollout batches + exemption registry
    ├── components/    # feature components (incl. TerminalPanel, TerminalView)
    ├── components/ui/ # shadcn-style Vue primitives + barrel
    ├── agentPacks/    # bundled App-owned manifests and connected-epoch bootstrap
    ├── debug/         # self-contained Agent Debug Studio feature
    ├── composables/   # shared app composables (incl. useTerminal)
    ├── locales/       # en / zh-CN i18n bundles
    └── api.ts         # renderer DTO mirror of Core/Gateway shapes

apps/TinadecUI/        # TinadecUI — UI engineering suite; import as '@tinadec/ui'
└── src/
    └── engine/        # TinadecUIE — deterministic layout engine (types/reducer/commands/undo/scope/registry/presets/repair/constraints/commandBus/instancePool) + components + cards + persistence
```

## WHERE TO LOOK
| Task | Location | Notes |
|------|----------|-------|
| Electron startup / app config | `electron/main.cjs`, `electron/preload.cjs`, `electron/appConfig.cjs` | Hardened renderer plus validated, main-process-owned Gateway URL persistence. |
| Local pet system | `electron/petStore.cjs`, `electron/petWindow.cjs`, `src/pets/petRuntime.ts`, `src/pages/DesktopPetPage.vue`, `src/pages/SettingsPage.vue` | Desktop-only Petdex v2 registry and transparent, always-on-top Canvas windows. `petRuntime.ts` is the renderer business module: it validates proportional 8-column sheets, maps the canonical nine Petdex state rows and active frame counts, loops with modulo, and calculates source rectangles. Petdex `pet.json` contains identity/path metadata, not animation definitions. Local files, enable state, bounds, and scale live under Electron `userData/pets/`; IPC is sender-scoped. Never call Gateway/Core. |
| Renderer bootstrap | `src/main.ts`, `src/App.vue`, `src/router.ts` | App is `RouterView`; routes lazy-load pages. `main.ts` installs `vaporInteropPlugin` and the global renderer error fallback. `App.vue` starts `OfficeAgentPack` checks on each main-renderer `connected` epoch; child/pet/debug windows skip them. |
| Main shell | `src/pages/HomePage.vue`, `src/components/*` | Chat, approvals, events, context, task graph. |
| Full-duplex chat integration | `src/controllers/HomeController.ts`, `src/api.ts`, `src/components/ChatPanel.vue` | Home `handleSend()` goes through `api.createInteraction()` (POST `/interactions`, snake_case) with no legacy fallback — the old `invoke-stream`/`POST messages` fallbacks were removed and `invoke-stream` is retired (404), so failures surface visibly. **Free conversations (2026-09-10)**: no project is required — `handleSend`/`createSession` pass `project_id: null`, the ComposerBar project dropdown offers 「自由对话 (无工作区)」 (`select-project` emits `string | null`, `setSelectedProject` accepts null), the AppSidebar lists projectless sessions under a 「自由对话」 group, `loadSessions` fetches the unfiltered list, and the New-Chat button is enabled with zero projects. **Contract repairs (2026-09-10)**: `SessionDto.project_id` is `string | null` (Core omits it; Gateway now maps it to `null` instead of `''`, so strict-equality comparisons in the renderer work); `HomeController.createSession` normalizes both sides before the pending-dedup compare (`(existing.project_id ?? null) === (projectId ?? null)`) — the raw check never matched and created a duplicate free conversation per click; `refreshProjectsAndSessions` also uses the unfiltered `listSessions()` (per-project queries never return free conversations, so archiving or trashing anything wiped them from the sidebar until a reload); the AppSidebar no longer hides the default `'Tinadec session'` title (a fresh conversation was invisible until its first message generated a title); ComposerBar shows 「自由对话」 rather than 「选择项目」 when no project is selected. Core freezes a manifest with only the `create_workspace` virtual tool for such sessions; approving it binds the session to the new workspace (`api.migrateSession` is the manual equivalent). Composer dispatch model (Codex-style): sends default to `queued`; when Core returns the transient `status:'queued' && !run_id` (session at active-run limit) the message becomes a local queued card above the composer (`HomeController.queuedMessages`) with 引导（insert+target_run_id）/并列（parallel）/编辑/取消 actions — nothing auto-runs it, cards are per-session and cleared on session switch. Enter/send-key default lives in Settings→General via `src/lib/dispatchPref.ts` (`tinadec.enter_pref`: `queued`(default)/`parallel`/`ask`; `ask` opens a small popover above the send button). The composer send button is NOT disabled while busy. Do not claim run controls, candidate review, or Core-driven mode selection is already wired. |
| Settings | `src/pages/SettingsPage.vue`, `src/settings/sections/*.vue` | Agent Center is merged into Settings with agents/modes/prompts/evolution/runtime tabs and versioned writes. Its compact Office Pack strip shows bundled/installed version, managed/read-only state and install/upgrade/retry/clone actions; managed agents open the existing clone flow. General no longer owns a local default topology; WorkspaceDefaults come from Core. |
| Backend service self-discovery | `electron/serviceDiscovery.cjs`, `electron/main.cjs` (`tinadec:discover-services`), `electron/preload.cjs`, `src/settings/sections/GeneralSection.vue`, `src/settings/settings.css` | General settings own the discovery UI (2026-08-28): the main process scans loopback + private NIC IPv4 on ports 48730–48739 (400ms TCP probe, then `/api/v1/health` fingerprint — `gateway:'ok'` → Gateway with embedded Core health, `name:'tinadec-core'` → Core direct-connect candidate shown informational only) and renders the list above the manual URL input; selecting a gateway candidate fills the draft, and save still applies after restart (no hot-swap of `api.gatewayUrl`). Keep `.spinning` neutralization inside the file's single trailing `@media (prefers-reduced-motion)` block — the CSS contract test splits on the first occurrence. |
| Office Agent Pack | `src/agentPacks/OfficeAgentPack/manifest.json`, `src/agentPacks/OfficeAgentPack/index.ts`, `src/agentPacks/officeAgentPackBootstrap.ts` | Static `tinadec.office.agent-pack@0.2.4` envelope (`manifest.json:metadata.version` and `OFFICE_AGENT_PACK_VERSION` must always agree, and the digest must be recalculated whenever either changes — Core rejects a re-published version whose bytes differ with 409 `agent_pack_version_hash_conflict`) (14 Agent + **5 prompt pipelines** — shared `baseline-prompt` plus role-discipline `meeting/planner/supervisor/worker-prompt` graphs bound per agent role — + **7 modes** — `default-mode` full baseline plus one topology per composer mode `conversation.plan/spec/ask/vibe/auto/agent` — + defaults). Bootstrap verifies preview pack_id/owner/version/hash before confirmation, uses exact preview + idempotency/ETag apply, keeps refusal lifecycle-local, exposes persistent Retry, and coordinates Web tabs with BroadcastChannel/Web Locks. Governance (`operation`) agents must ship `tool_scope: []` — Core denies every tool invocation for an operation-layer instance and `OfficeAgentPack.test.ts` pins that as a data contract; the RFC 8785 digest in `index.ts` is recalculated on every manifest edit. |
| Composer mode → Core mode wiring | `src/controllers/HomeController.ts` (`handleSend`/`promoteQueued`), `src/api.ts` (`createInteraction agent_mode`/`mode_version_id`), `src/components/ModeSelector.vue` | ONE mode entry point: the composer toolbar's `ModeSelector` (rendered in BOTH hero and docked states). Its dropdown merges 跟随默认 with the workspace's published ModeVersions (`listAgentModeTopologies`; `· 默认` suffix marks published). Selecting a version pins `mode_version_id` — sent on the next submit in both states (hero `welcome-submit` carries it too), and Core no longer accepts an `agent_mode` at all; 跟随默认 clears the pin and the send falls back to the workspace default mode that Core resolves (`tinadec.agent_mode` survives only as a local preference: the six-value enum has left the contract - Core answers 400 `unknown_field` and Gateway's `interactionsMapper` rejects it locally before the hop). With zero published versions (pack not installed) the dropdown offers only 跟随默认 - there is no bare enum left to fall back to. `modeVersionId` state is owned by `ChatPanel` (reset from `currentSession.mode_version_id` per session). The old `SessionModeStrip` above the composer is deleted. Core validates an explicit `mode_version_id` as a published ModeVersion and persists it onto the session (`UpdateSessionModeAsync`) — the mid-conversation override path. Insert-steering into a frozen run still sends none. |
| Unified persistent composer (2026-09-05) | `src/components/ChatPanel.vue`, `src/components/ComposerBar.vue`, `src/components/WelcomeScreen.vue`, `src/lib/dropdownPlacement.ts`, `styles.css` | There is ONE composer element for the whole conversation lifecycle: `ChatPanel` mounts `ComposerBar` outside the `chat-panel` `<Transition>` (which now swaps only the hero title block vs the active body, opacity-only, leaving branch lifted out of flow). `WelcomeScreen` is the hero title row only — no input box, no toolbar, no local draft. `ComposerBar` takes `hero` (start page): centered via `.conversation.composer-hero`, submits `welcome-submit {content, agent_mode, permission_mode, mode_version_id}`; docked submits the dispatch `submit` payload with queued cards/ask-menu. The box keeps classes `.composer-box .welcome-dialog` and inherits ALL box visuals (12px radius, `--shadow-panel`, `--surface-input`, blur material) from the shared `.welcome-dialog` rules — `styles.css` must NOT redeclare `.composer-box` visuals (`settingsCssContract.test.ts` pins this). `.composer` wrapper is `max-width: 852px` (820 dialog + 2×16 gutters) so hero/docked box widths match and the dock FLIP is a pure vertical translate: `ChatPanel` watches `hero`, captures the box rect pre-flush, and WAAPI-animates `translateY` (0.35s `cubic-bezier(0.4,0,0.2,1)`, transform on the box itself — never an ancestor of the backdrop-filtered surface; skipped on zero rects / reduced motion). Popovers (plus menu, project dropdown, ask-menu) all Teleport to body via `computeDropdownPlacement` — space-aware flip, so docked menus open UPWARD. `loadMessagesAndApprovals` keeps `pending-*` optimistic messages until the backend echoes them (content match) — without this the session-select reload racing the first POST empties the list and bounces the composer mid-sink instead of one immersive sink. Pinned by `ChatPanel.test.ts` (element persistence via dataset probe), `ComposerBar.test.ts` (hero variant, teleported ask menu, merged selector), `dropdownPlacement.test.ts`. |
| Mode topology panel | `src/settings/sections/AgentModesPanel.vue`, `src/components/canvas/AgentModeCanvas.vue` | Modes tab renders every workspace mode; pack-managed/published modes read their published projection (`managed` flag) and render inspect-only (no drag/connect/edit, clone entry via `cloneSelectedMode`), drafts stay editable. |
| Runtime center view adapter | `src/runtimeCenterView.ts` | Converts Gateway center DTOs into provider forms, topology labels, and runtime-source presentation without persisting binding state. |
| CLI quick-connect | `src/api.ts` (`connectCliRuntime`), `src/pages/SettingsPage.vue` (`connectDiscoveredCli`) | Discovery candidates render from `GET .../cli/discover`; the quick-connect button POSTs `connect` directly (Core spawns/reuses the CLI service and persists an enabled provider), then refreshes the model center. Manual provider editing still runs through the provider form/modal. |
| Provider presentation templates | `src/providerTemplates.ts` | Presentation-only metadata (i18n keys, brand colors, placeholders, icons). Brand icons are official `@lobehub/icons-static-svg` SVGs imported via Vite `?raw` (23 drivers); drivers without a lobehub slug (`sglang`, `llamacpp`, `custom`) keep hand-written `currentColor` SVGs. `icon` is an inline `<svg>` string rendered via `v-html` inside `.provider-brand-icon`/`.modal-provider-logo` (24px/32px CSS sizing). |
| Prompt Context settings | `src/settings/sections/PromptContextPanel.vue`, `src/api.ts` | Manage/clone custom prompt fragments and preview Core-assembled prompts through Gateway; do not assemble prompts in the renderer. |
| Tool layer catalog/search | `src/pages/SettingsPage.vue`, `src/toolCatalog.ts`, `src/api.ts` | Settings presents Code-suite tools, Codex primitives, supported runtimes, Core manifest registry governance/design notes, and Core-owned tool search results. |
| Tool execution visibility | `src/pages/HomePage.vue`, `src/components/ContextPanel.vue`, `src/components/OrchestrationTab.vue`, `src/api.ts` | Right rail presents Core-owned tool execution timeline state, provider layer, duration, checkpoint summary, and step-result evidence. |
| Git management UI | `src/components/GitPanel.vue`, `src/components/git/*`, `src/composables/useGitOperation.ts`, `src/userToolAction.ts`, `src/api.ts` | Island-style card design (2026-08-30): GitPanel header/nav are top-level islands (may carry `--material-filter-section`), the four collapsible Changes sections and every sub-view (branch list, worktrees, compare, history timeline/detail) are `UiIslandCard`s, stage actions are the files island's footer. Right rail Git tab calls Gateway previews for reads. Every stage/unstage/commit/push/checkout/branch/worktree/merge/rebase/conflict write creates a Core UserToolAction with stable idempotency and renders its durable state; it never creates an approval locally or runs Git directly. |
| Marketplace | `src/pages/MarketPage.vue` | Extension source/catalog/install flow. |
| Debug Studio | `src/debug/DebugStudio.vue`, `src/debug/**` | Composables/types/components are feature-local. |
| UI primitives | `src/components/ui/index.ts`, `src/lib/utils.ts` | `Ui*` barrel exports; `cn()` uses clsx + tailwind-merge. `UiIslandCard` (`island-card.vue`, 2026-08-30) is the floating-card primitive: `variant: section|raised`, `padding: none|sm|md`, `hoverable`, `divided`, header/default/footer slots; it consumes inherited `--surface-*` tokens and is never a material root (no `getPanelStyle()` binding). |
| Appearance/i18n | `src/composables/useTheme.ts`, `src/composables/usePanelStyles.ts`, `src/composables/useDynamicPalette.ts`, `src/lib/monetExtract.ts`, `src/components/ui/color-field.vue`, `src/i18n.ts`, `src/locales/*` | Persisted theme/accent/locale plus synchronous normalized global panel material behavior. Custom accent picker (2026-09-10): the OS-drawn `<input type="color">` is replaced by `UiColorField` — a hand-written, dependency-free 2D saturation×lightness area (`role="slider"`, pointer capture + arrow/Home/End keys, `aria-valuetext` = sat/light pair) whose lightness overlay must be set as `background-image`, not the `background` shorthand, because DOM implementations that keep only the last shorthand layer silently drop the black→transparent→white ramp (`color-field.test.ts` pins it). The picker keeps only the hue range input (now with a spectrum track) plus hex field and eyedropper; saturation/lightness sliders were removed because the 2D field supersedes them. The custom swatch (9th) is two-state: with no custom color active its dot renders a hue wheel (`.accent-color-dot--spectrum`, `conic-gradient` across the full hue range) plus a white bolder plus (16px, `stroke-width:3`, white stroke + drop-shadow so it reads on every hue) to advertise "pick anything here"; once custom is selected it reverts to a solid `--swatch-color` dot with a check and the plus disappears. Unified color pipeline (2026-08-24): all 8 presets (seeded from their `dark.accentPrimary`) and the 9th `dynamic` value (seeded from the wallpaper-extracted `sourceColor`) run through the same Monet builder — `monetExtract.ts` (deterministic Wu+Score over `@material/material-color-utilities@0.3.0`; never `QuantizerCelebi`, its K-means `Math.random()` would desync cross-window palettes) — and inject the same ~59-var full-surface set per theme, including the shadcn HSL triplet family (`--background/--primary/--card/--popover/--accent/--border/--input/--ring`…) so body base, UiButton/badge/checkbox/switch/progress/tooltip and `bg-card`/`bg-popover`/`bg-accent` utilities all follow the active color instead of fixed hue-222. Preset combos are memoized `key:theme`; dynamic rebuilds on each palette update. `useDynamicPalette.ts` silently extracts when an image background is set (selection never switches the accent), caches under `tinadec-dynamic-palette` (`StorageSerializers.object` — the null-default "any" serializer returns raw JSON strings), freezes on video/html/none. Settings renders the dynamic option as a pill closing the preset swatch row (direct child of `.accent-color-grid`, `margin-left: auto` right-aligns it; wraps to its own right-aligned line on narrow windows) carrying **one** swatch — `dark['--accent-primary']`, the same role the presets show — because the previous five-circle role strip (`previewSwatches`: accent/button/surfaces) read as a palette next to single-color swatches; a bare circle would be indistinguishable from a preset, so the pill keeps its label. `previewSwatches` is still used for the custom picker's draft preview. Local images load through the `tinadec:read-image-data-url` IPC (dev renderer is http-origin; direct file:// canvas reads are tainted). Extraction hard rules: pack pixels with the `0xff<<24` alpha byte and read alpha from `data[i+3]` (RGBA), and `await img.decode()` after onload — QuantizerWu skips alpha<255 pixels and onload races the decoder, and dropping either makes extraction silently fail (option permanently disabled). Failures notify via useNotifications (deduped per source). Status/error semantic colors are never tinted. |
| Unified notifications | `src/composables/useNotifications.ts`, `src/components/NotificationIslandHost.vue`, `src/components/NotificationDetailDialog.vue`, `src/App.vue`, `electron/main.cjs`, `electron/preload.cjs` | Three kinds (`transient`/`status`/`task`) render in separate title-bar island zones; clicking a capsule opens the detail dialog, while hover expands a glass card that also opens the dialog when clicked. Repeated notifications merge with a counter; overflow opens the notification center with live + cleared sections. `status.*` replicates across renderer windows over IPC. System load/connection errors use islands; chat approvals stay contextual. |
| Detached panel windows | `electron/panelWindow.cjs`, `src/pages/DetachedPanelPage.vue`, `src/components/ContextPanel.vue`, `src/composables/usePanelTabs.ts` | Electron multi-window management: BrowserWindow creation, cursor-polling drag-to-detach (Chrome-style tab tearing), disk-based layout persistence, reattach/focus, and cross-window theme broadcast. Main window is tagged with `_isTinadecMain` so `getMainWindow()` distinguishes it from Debug Studio. Panel layout persisted to `~/.tinadec-panel-layout.json` on move/resize/quit and restored on launch. |
| Integrated terminal | `electron/terminalManager.cjs`, `electron/preload.cjs`, `src/composables/useTerminal.ts`, `src/composables/useTerminalSource.ts`, `src/components/TerminalPanel.vue`, `src/components/TerminalView.vue`, `src/components/chat/TerminalCallBlock.vue`, `apps/TinadecUI/src/components/cards/home/TerminalCard.vue` | One widget, two transports: the user's own shell is a `node-pty` PTY in the main process, an agent shell replays Core's event journal and writes back over audited HTTP. Multi-instance tabs, absolute-path shell catalog, xterm.js themed from CSS variables, shortcuts (Ctrl+Shift+T / Ctrl+W / Ctrl+Tab), guarded auto-fit, detachable panel windows. No silent pipes-only fallback — an unspawnable PTY shows a persistent retryable reason. npm workspaces hoist `node-pty` to the repository root, so packaging maps `../../node_modules/node-pty` in and `asarUnpack`s it. `usePanelTabs`/`ContextPanel` are Debug Studio survivors, not the live host. |
| TinadecUIE layout engine | `apps/TinadecUI/src/engine/**`, `apps/TinadecUI/src/components/**` (import `@tinadec/ui`), `src/controllers/*.ts` | Deterministic 3-slot (`left/center/right`) layout authority (see `apps/TinadecUI/AGENTS.md`). Engine module (`apps/TinadecUI/src/engine/` — pure TS: `types/commands/reducer/undoStack/scope/registry/presets/repair/constraints/commandBus/instancePool`) owns layout state; Components module (`apps/TinadecUI/src/components/` — `UieShell`/`UieCanvas`/`useUie`/cards) only reads snapshot → geometry → DOM. All mutations go through `commandBus.dispatch({command, source, expectedRevision})`; `ai` source is reserved/rejected. Home renders via `UieShell`; card instances hydrate once by `instanceId`. Geometry applies a snapshot-level `edgeInset` to both window edges (float pages home/settings = 8, app pages market/code/debug = 0) and `BOTTOM_INSET = 8` to the bottom. The constraint solver keeps the adaptive center column at least `MIN_CENTER_WIDTH = 320`; under space pressure it collapses the right rail first, then the left (visual only) so the chat zone never gets crushed. Per-column `SurfaceMode` is `float` (rounded/shadowed material root), `app` (flush connected layout, market/code/debug), or `immersive` (transparent zone — the Home chat column): an immersive stack's root carries no material so the page background shows through, but it keeps `data-panel-effect` so inner objects (composer, welcome dialog, bubbles) inherit the remapped surface tokens and follow the global panel material. Per-page domain controllers (`HomeController`/`CodeController`/`MarketController`) are module singletons that share SSE/WS/data across cards. |
| TinadecUIE persistence | `electron/layoutStore.cjs`, `apps/TinadecUI/src/engine/persistence/**` | Layouts persist to `userData/workbench-layout.json` (atomic temp+rename, IPC `tinadec:layout-load/save`). Renderer `layerStore` resolves `workspace-page > page > global` per page; auto-save debounced 400 ms and flushed on `beforeunload`/`pagehide` so Ctrl+R never loses the last change. `repairLayout()` never shows a blank window; the hydrate/restore promise has a `.catch` that keeps the built-in preset on failure. Snapshots carry `edgeInset` (8px left/right for float pages home/settings, 0 for app pages market/code/debug); `repairLayout` defaults it by `pageId` so legacy JSON migrates automatically. Legacy `~/.tinadec-panel-layout.json` PanelTypes migrate via `migrate.ts`. |
| Vapor mode | `src/lib/vue-shim.ts`, `src/vapor/**`, `src/main.ts` | Vue 3.6 RC Vapor per-SFC via `<template vapor>`; root `overrides` pin `vue`/`@vue/compiler-sfc` to `3.6.0-rc.7`. `vue-shim.ts` re-exports runtime-dom + runtime-vapor so vapor SFCs resolve in build and vitest; `main.ts` installs `vaporInteropPlugin`. Rollout batched in `vaporBatch.ts`; exemptions in `VaporExemptions.ts`. `AppSplash.vue` is exempt (not vapor): the root `splash-exit <Transition>` must be classic-around-classic, since a classic Transition wrapping a Vapor SFC exercises the classic↔Vapor interop leave path that crashed on Ctrl+R reload. |

## CONVENTIONS
- Agent Center sub-panels share the `workbench-duo` two-pane skeleton: `.center-workbench.workbench-duo` with `.center-resource-rail` / `.center-resource-stage` children (geometry and 1100/700 breakpoints live in `settings/settings.css`). Each center has exactly ONE Refresh button, in the shell command bar, dispatched by `refreshAgentCenterTab()`; panels expose their loader instead (`loadModes`/`refreshAll`/`loadProposals`/`loadRuntimeInstances`) and never render their own refresh buttons. Version/history rows use the shared `.center-list-row` primitive (settings.css), not per-panel chrome.
- Use `@/*` for imports from `src/*` when it improves clarity.
- Windows system surfaces use `public/tinadec.ico`: main, Debug Studio, and detached `BrowserWindow` instances must reference it in both dev and built `dist`; keep `app.setAppUserModelId('com.tinadec.office')` for taskbar grouping.
- Main and Debug Studio windows use `titleBarStyle: 'hidden'` without `titleBarOverlay`, preserving the custom controls while leaving the native frame available for Windows DWM corners and shadows. Detached panels remain frameless because their drag and window-control hit testing depends on the custom title bar; pet windows remain transparent and frameless.
- Router uses `createWebHashHistory()`; routes: `/`, `/settings`, `/agent-center`, `/market`, `/debug-studio`, `/code-editor`, `/workbench`, `/recovery/:actionId`, `/governance`, `/snapshots`, `/library`, `/panel` (detached panel window), `/pet` (transparent local pet window).
- Debug Studio and detached panels load with `?splash=0`: `App.vue` must skip connection polling and route transitions for them, global CSS must reset the main window's root minimum size, and `.main-content` must remain above `.background-layer` and clip page-transition overflow. Failed Debug Studio renderers are destroyed so the next open recreates them.
- Pinia is installed (`src/stores/`: project/session/run/workbench). Prefer a store when state must be shared across components or survive route changes; composables and local refs remain fine for component-local or module-singleton state (e.g. `useTerminal`, controllers).
- `usePanelStyles()` owns the global panel material under `tinadec-panel-style`. Its normalized `{ effect, opacity, blur }` state uses a detached module-lifetime `useStorage` scope with synchronous writes, so page unmounts, renderer reloads, and Desktop restarts preserve the selected material.
- Material roots carry `data-panel-effect`; descendants inherit the active `opaque`/`translucent`/`blur` `--surface-*` values and must consume those role-based tokens directly for neutral backgrounds, using scoped Tailwind utility remaps only as a compatibility fallback.
- Settings content keeps only its navigation and content outer frames. Inside those frames, use `--surface-section`/`--surface-chrome`/`--surface-raised`, spacing, hover, and selected states before neutral decorative borders; preserve functional input/focus boundaries, semantic warning/error/risk accents, and modal elevation. `--material-filter-section` and `--material-filter-raised` are reserved for a few top-level groups, never repeated list items.
- Island cards (`UiIslandCard`, `src/components/ui/island-card.vue`) are floating sections separated by spacing + card border + subtle shadow, not by decorative dividers. They are descendants, never material roots — do not bind `getPanelStyle()`/`data-panel-effect` inside them or nested `backdrop-filter`s stack. Neutral backgrounds come from `--surface-section`/`--surface-raised`; git status semantics use the fixed, never-tinted `--accent-*`/`--text-*` tokens via `color-mix(... transparent)`, never raw hex. Only the GitPanel header/nav and other top-level islands may consume `--material-filter-section`; repeated rows and list items must not.
- Text selection is unselectable by default: `styles.css` sets `user-select: none` on `body`, and only content surfaces opt back in through one explicit selector list (`input`, `textarea`, `.message-content`, `.markdown-body`, `.detail-dialog`, `.composer-error`, `.tool-call-card`, `.tool-result-viewer`, `.thinking-process`, `.terminal-call-block`, `.commit-compare`, `.diff-viewer`, `.inspector-panel`, `.search-results`, `pre`, `code`); a second rule forces `button`/`label`/`summary`/`[role=button|tab|menuitem|option]` back to `none` so chrome nested in a selectable container stays unselectable, and a third re-enables `text` for the whole `.island-host` subtree because notification capsule bodies are themselves `<button>`s. New selectable content must join the opt-in list **and** `SELECTABLE_CONTENT_SELECTORS` in `lib/selectionContext.ts`; the guards are `settingsCssContract.test.ts` and `selectionContext.test.ts`. Monaco (its own `no-user-select` class) and xterm declare their own values, so neither is affected.
- Right-click on text is handled by the in-app `SelectionContextMenu.vue` (mounted once in `App.vue`, like `NotificationIslandHost`) — not a native Electron menu. It listens on `document` in the bubble phase and returns early on `event.defaultPrevented`, so the **three live** right-click owners keep their own right-clicks: `AppSidebar` project/session rows (`RowContextMenu`, 重命名/归档/删除 — reached through the TinadecUI `nav` card, NOT a route page, so a `pages/`-only grep wrongly reads it as dead), `FileTreePanel` rows (inline path menu, `/code-editor`), and `BrowserTabBar` feature tabs (detach; the pinned Home tab returns early). `ContextPanel`'s tab handler is the only dead one (Debug-Studio preview only). `lib/selectionContext.ts` resolves the surface (`terminal | field | content | chrome`) and the action matrix: content gets copy/select-all, editable fields add cut/paste, read-only fields never do, terminals get copy/paste/select-all/clear through xterm's own API (it paints to canvas, so the DOM selection is always empty there), and plain chrome with no selection opens nothing. Link actions only ever accept `http(s)`. Clipboard goes through the `tinadec:clipboard-read-text`/`-write-text` IPC (main-process `clipboard`) because `navigator.clipboard.readText()` needs a permission grant the dev http origin lacks; the browser build falls back to `navigator.clipboard` (`webShim.ts` leaves both bridges undefined). The menu deliberately has **no** open/close transition: `Teleport` + `<Transition>` hits the documented Vue 3.6-rc.7 + happy-dom null-anchor bug (see `NotificationIslandHost.test.ts`), and a menu opened many times a day should not animate anyway.
- `useNotifications()` is the only Desktop system notification API, organized in three semantic kinds: `notify.*` for short-lived transient feedback, `status.*` for persistent keyed state-driven conditions that replicate across all renderer windows, and `notify.task()` for long-running operations that settle in place. `banner.*` is a backward-compatible alias for `status.*`. `confirm()` shows a confirmation dialog; `dismiss`/`dismissByKey`/`dismissAll` remove items. Clicking a capsule opens the detail dialog directly; hover expands the glass detail card after an intent delay, and clicking that card also opens the dialog. Closing the card or detail dialog does not dismiss the notification. Dismissal follows the explicit lifecycle contract in `useNotifications.ts` (`NotificationPersistence` + `isUserDismissible`, the single source of truth for every close affordance): transient feedback auto-expires and is user-closable; `persistence: 'sticky'` transients never expire but stay user-closable; pending tasks are handle-owned and never user-closable until they settle into auto-expiring feedback; `status.*` conditions are source-owned and never user-closable — the owning state source clears them with `dismissByKey` when the condition recovers, and incidental local removal (cap eviction, programmatic `dismiss`) never broadcasts a clear to other windows. `dismissAll` clears only user-closable items. System load failures and backend connection state use `status.*` islands — never `error-strip` red banners. Keep field validation, diagnostics, Git conflicts, and agent/tool approval cards contextual. Core approvals and Tool `confirm_*` remain authoritative.
- Notifications with an identical key (or identical kind+level+title+message) merge in place with a repeat counter instead of stacking. Transient items auto-expire even while queued off-screen; only direct user attention (hover, open card, open detail) pauses their timer. Sticky transients, status items, and pending tasks never auto-expire.
- Transient and status notifications render in separate island zones, so a standing condition can never starve transient feedback. Items beyond the visible three are reachable through the notification center, which pairs the live list (source-owned rows show a residency marker instead of a close button) with the cleared-history ring.
- Notification state is per-window EXCEPT `status.*`, which is broadcast over Electron IPC (`tinadec:broadcast-status-notification` → `tinadec:status-notification`) to the main window, detached panels, and Debug Studio, excluding the sender and pet windows. Actions cannot cross IPC, so replicated items are flagged `remote` and render without an action button. Standard windows use the title-bar corridor; detached/Debug place islands below occupied title bars; pet windows never mount hosts.
- UI stack: Vue, Tailwind via `@tailwindcss/vite`, lucide-vue, `@lobehub/icons-static-svg` (provider brand icons only; framework-agnostic SVG files, imported `?raw` in `providerTemplates.ts`), shadcn-style primitives.
- Tests are colocated `src/**/*.test.ts`; command is `vitest run`.
- Prompt Context UI is presentation and local preview only. The renderer calls Gateway APIs mirrored in `src/api.ts`; Core owns fragment selection, context pack handling, token estimates, and warnings.
- The canonical dual-agent layers are `operation` and `execution`; `planning` is a Core migration alias, not a Desktop-owned mode. Desktop must render the application/agent modes returned by Core (including `conversation` defaults and `space`'s `agent`-only restriction), rather than hard-coding the six-mode set or interpreting profile policy locally.
- Composer dispatch affordances stay out of the send box: no persistent 并行/排队/插入 buttons. Dispatch intent is expressed after sending (queued card actions) or in Settings→General (`dispatchPref.ts` is the single owner of the `tinadec.enter_pref` key — never read/write it elsewhere).
- Normal user chat goes through Core's `POST /api/v1/sessions/{id}/interactions` contract (output is read back via `GET /api/v1/runs/{runId}/stream`); `invoke-stream` is retired and returns 404. Never `POST /messages` and then invoke the same content: the invocation owns idempotent user-message creation.
- A local `AbortController` only stops the renderer's SSE reader; it is not a run cancellation. Pause, resume, cancel, target changes, approval resumption, mode changes, context versions, agent lineage, and memory/agent-candidate review must be requested through Gateway/Core and displayed from their durable projections.
- Full prompt bodies, long-term-memory writes, agent spawning, tool authorization, approval consumption, and state revisions remain Core responsibilities. Desktop may show progress, candidate evidence, and control affordances but must not synthesize durable state or promote a candidate locally.
- Gateway URLs must use HTTP or HTTPS and contain no credentials, query, or fragment. Keep all renderer requests on `window.tinadec.gatewayUrl()` / `api.gatewayUrl`; do not add localhost request bypasses.
- Model Center composes versioned provider templates/instances/routes/readiness directly through Gateway and renders Core suppliers, API/local connections, configured models, CLI runtimes, and ACP runtimes. `providerTemplates.ts` may only supply presentation metadata such as translations, icons, colors, and placeholders. `runtimeCenterView.ts` must carry `revision` through **every** provider projection (`apiConnectionProvider`, `cliRuntimeProvider`, and the CLI/ACP rows in `aggregateModelCenterOverview`) — it is the `If-Match` token `saveModelProvider`/`saveModelRoute` send, and dropping it makes Core answer `428 precondition_required`. `loadModelCenter` must also assign `routes.value` from the fetched rows: the Routes tab, the route editor, and `setDefaultChatModel` all read that ref, so a shadowing local leaves them permanently empty. Both are pinned by `runtimeCenterView.test.ts` (`carries the provider revision through...`) and `SettingsPage.smoke.test.ts` (`assigns the routes ref from loadModelCenter...`).
- Canonical live discovery is `POST /api/v1/model-providers/{id}/models/refresh`; Core holds credentials, Desktop confirms the discovered merge, and Gateway only proxies. Diagnostics remain visible and retryable without hiding usable partial data.
- Agent Center composes versioned Agent/Mode/Prompt/WorkspaceDefaults/Pack APIs through Gateway. Agent writes use `updateAgentDraft → publishAgent`; never persist effective topology/defaults in Desktop, Gateway, or `localStorage`.
- `OfficeAgentPack` is an App artifact, never a renderer-authored profile write. Retry always obtains a fresh preview; Core remains the concurrency authority. Pack-managed resources are read-only, and customization starts with clone/create of a user-owned resource.
- Legacy `model_route_purpose` bindings can be shared by multiple agents. Show `LEGACY_SHARED_ROUTE` warnings and never save an agent runtime choice by rewriting the shared model route.
- Code-suite UI is presentation-only: group/filter tool descriptors and project template summaries from Gateway/Core. Read-only explicit user queries may use `/api/v1/code/tools/{toolId}/execute` or `/api/v1/tool-runtime/*`; mutating actions use `api.createUserToolAction()` and `resume`/snapshot override. Agent executions use the Core-owned `/api/v1/runs/{runId}/tools/{toolId}/execute` path instead.
- Git UI is presentation plus Core UserToolAction execution: request Tool-layer previews from Gateway, route every mutation through `/api/v1/user/tool-actions`, and display `snapshot_required`, `awaiting_delegate`, `awaiting_user`, `awaiting_approval`, `running`, `completed`, `blocked`, and `outcome_unknown`. Desktop never creates `/api/v1/approvals`, computes risk/parameter hashes, or handles nonce material.
- Tool search UI must consume Core/Gateway `/api/v1/tools/search` results. Do not invent provider-layer, matched-field, or human-checkpoint semantics in the renderer.
- Tool execution UI must consume Core/Gateway `/api/v1/sessions/{sessionId}/tool-executions` results. Do not reconstruct audit timelines, provider layers, durations, or checkpoint summaries from local event arrays in Desktop.
- Dev server is pinned: `127.0.0.1:5173`, `strictPort: true`.
- Vite `base` must remain `./` because packaged Electron windows load `dist/index.html` through `loadFile()`.
- Terminal backend is `node-pty` only. There is no implicit `child_process.spawn` downgrade: pipes mode has no echo, no prompt and no resize, so it looked identical to a dead terminal while hiding the reason. `terminal:create` returns the spawned `backend`, or `id: null` plus an `error` that the panel renders persistently.
- **Anything crossing `window.tinadec.*` must be plain data.** Electron serializes IPC with V8, which refuses Proxy objects, while a deep-reactive value still reports `Array.isArray(...) === true` — so a store value passed straight into a payload fails at the call site with only `"An object could not be cloned."` and no stack worth reading. Copy at the boundary (`plainStringArray`/`plainText`, `shallowRef` stores) and let `useTerminal.test.ts` keep proving it via `structuredClone`.
- Terminal IPC channels: `terminal:create`, `terminal:write`, `terminal:resize`, `terminal:destroy`, `terminal:snapshot`, `terminal:get-shells`, `terminal:list`. Data/exit events use per-terminal channels: `terminal:data:{id}`, `terminal:exit:{id}`. Output is coalesced per terminal, reaches only windows tagged `_isTinadecMain`/`_isTinadecPanel` (never pet or Debug Studio), and main keeps a bounded replay buffer so a view that attaches after spawn replays the first prompt before listening.
- Terminal state is managed by `useTerminal` composable (module-level singleton, therefore per renderer process). The Uie `terminal` card is also a **singleton**; multiple shells belong inside `TerminalPanel` tabs, never in multiple TerminalCard instances. A `TerminalView` unmount **detaches** rather than destroys: closing a tab, collapsing the feature column, a route change and a detach all unmount it, and killing the user's running shell for those was wrong. Detach is attachment-owner guarded so a stale unmount cannot dispose a newer view's xterm renderer. xterm `Terminal` and addons stored in Vue state must be `markRaw`. Only explicit close/restart/Ctrl+W destroy the PTY. Cross-window PTY ownership transfer (reconnect to the same id with scrollback) is not implemented; a detached window starts its own shell.
- Chat message visual language (Home conversation): user messages are borderless soft-fill bubbles — shape comes from `--bg-user-msg` (low-alpha `color-mix` teal tint) plus `--shadow-user-msg`, never a border line; long prompts clamp to `max-height: 12rem` with in-place scroll. Assistant messages carry NO agent-name label; their only meta is a timestamp at the END of the message (`.assistant-message-meta`), revealed on message hover (OpenCodeUI/Codex pattern). Assistant-side activity (`chat/ThinkingProcess.vue`, `chat/ToolCallCard.vue`) renders as flat single-line disclosures with no borders or status badge pills — status is carried by glyph color only. Shared micro-interactions live in `styles.css`: `.chat-shimmer` (Codex-style text-clipped sweep while running; hover freezes to solid), `.chat-status-rise` (Claude-style 6px rise on status/step change, replayed via `:key` swaps), `.chat-collapse` (grid-template-rows `0fr↔1fr` expand/collapse, no max-height hacks), `.activity-glow-icon` (currentColor double drop-shadow for active glyphs). Tool cards auto-expand while `running`/`waiting_approval` and auto-collapse ~800ms after completion unless the user toggled them manually. All motion has `prefers-reduced-motion` fallbacks. Approval affordances (批准/拒绝 row, 高风险 tag) must never be visually weakened by these simplifications.
- Debug Studio 预览画廊使用真实控件（2026-08-25 重构）：`preview/apiBridge.ts` 在画廊挂载期间把 `createMockApi(scenarioRef)` 补丁到 `@/api` 单例（引用计数 install/restore；未模拟方法走命名约定空实现并记录到 `unmockedMethods`，右栏展示「未模拟 API」chips）——依赖 api 方法全部为调用点属性访问、无顶层解构的前提，且仅影响 Debug Studio 独立渲染窗口。`PagePreview.vue` 的 GitPanel/CodePage/SettingsPage 直接挂载真实组件/页面（defineAsyncComponent 保持分包），MarketPage 经 `RealMarketPage.vue` + `uieBridge.ts ensurePreviewUie()` 以**无持久化**引擎初始化（绝不写 workbench-layout.json）；`PreviewNavScope.vue` 重提供 `routerKey` 门面阻断被嵌页面的 `router.push('/')` 逃逸。对话示例数据：`debug/preview/mockData.ts` exports `mockThinkingSteps()` / `mockToolCalls()`（deterministic，cover every step type + severity and all five tool-call statuses incl. a high-risk waiting_approval item）— page previews (`PagePreview.vue` HomePage/ChatPanel branches), component previews (`ComponentPreview.vue`), and the dedicated `ConversationFlow` 对话消息流 entry (`ConversationPreview.vue`, registered in `PreviewGallery.vue` PAGES) all consume these factories. `MessageList.vue` accepts an optional `activityByMessage` map (`Record<messageId, { thinkingSteps, toolCalls }>`) that overrides the shared arrays per message — the real Home chain still uses the shared arrays.

## ANTI-PATTERNS
- Do not call Core directly from renderer; call Gateway (`48730`).
- Do not expose filesystem/shell/model API keys to renderer; use preload/Gateway/Core boundaries.
- Do not add app-wide state store without checking existing composable/local-ref pattern.
- Do not duplicate route shells: `src/pages/DebugStudioPage.vue` is router-used; `src/debug/pages/DebugStudioPage.vue` appears alternate/unused.

## MCP TOOLS (vue-mcp) — USE PROACTIVELY

The dev server exposes a live Vue introspection server via `vite-plugin-vue-mcp`. When it is available, use it to inspect the *running* app instead of guessing from source, especially for UI work:

- **Use it when** you need the actual runtime picture: component hierarchy, a component's current state, registered routes, or whether a UI change renders as intended. `get-component-tree` and `get-component-state` answer "what is really on screen" faster than reading source.
- **Tools**: `get-component-tree` (live hierarchy), `get-component-state` (`componentName`), `edit-component-state` (`componentName`, `path`, `value`, `valueType`), `highlight-component` (`componentName`), `get-router-info` (registered routes), `get-pinia-tree` / `get-pinia-state` (`storeName`). Pinia stores live in `src/stores/` (project/session/run/workbench).
- **Prerequisites**: dev server running (`npm run dev:desktop`) AND the app loaded in the Electron window (or a browser against the dev server) AND Claude Code connected to `vue-mcp` (root `.mcp.json`, SSE `http://localhost:5173/__mcp/sse`). Tools return empty/stale results if the app page is not open.
- **Fallback**: if the `vue-mcp` MCP server is not connected, read the source under `src/` instead — never report "no components" as a fact when you simply lack a live connection.
- **UI automation (chrome-devtools / electron)**: `scripts/dev.mjs` launches dev Electron with `--remote-debugging-port=9222`; `electron-mcp-server` (root `.mcp.json`/`opencode.jsonc`) auto-attaches there for window info, screenshots, UI interaction, eval, and logs. `chrome-devtools-mcp` can attach to that CDP endpoint too (`--browser-url http://127.0.0.1:9222`) or drive the plain Vite renderer at 5173 in Chrome for renderer-only checks.

## COMMANDS
```bash
npm install          # 在仓库根目录；.npmrc 已设 legacy-peer-deps=true 以兼容 vite-plugin-vue-mcp 的 Vite 7 peer 范围
npm run dev -w @tinadec/desktop
npm run build -w @tinadec/desktop
npm run test -w @tinadec/desktop
npm run rebuild:native -w @tinadec/desktop  # rebuild node-pty for Electron (requires Python)
```

## NOTES
- **GraphSeedPack 2.4.0 工具面与四模式（2026-09-19 起）**：`search` 模板声明 `ls/stat/read_file/file_search/git_status/git_diff/git_log/git_branch_list/mcp_search/mcp_invoke`，`global_engineering` 在其上叠加 `write_file/shell/git_commit/git_push`；`meeting` 保持 `[]`（“闸①禁止 operation 层持有工具”已于 2026-09-17 全局撤下：`ModePublishGate` 的 `operation_tool_floor_violation` 与 `CoreAuthorizationContextResolver` 的 `operation_layer_cannot_invoke_tools` 均已不存在，`ToolChainEndpointTests` 反向钉住其缺席。但**仍然不要**把这些工具加到 `meeting` 上——solo 档的判据就是“对话身份声明了非空工具面”，加上去会把已有三个模式一齐判成 solo；第四模式 `solo` 因此用独立的 `solo_master` 承载）。。**TinaChat 六个会话工具（2.4.0）声明在 `solo_master` 与 `global_engineering` 上**（`tina_chat_bind`/`search_people`/`list_rooms`/`read_inbox`/`send`/`propose_intent`），让主人和被派出的工程执行体都能在聊天室里以具名身份读写；`search` 刻意不持有。它们是 Core 虚拟工具，不进 `WorkspaceToolCatalog`（`MutatesWorkspace=false`），因此不触发规则③的写授权要求。模板 system_prompt 已写明"路径参数传工作区内绝对路径，工作区根见运行时注入的工作区段"。**改 manifest 必须三处同步**：`GraphSeedPack/index.ts` 的 `GRAPH_SEED_PACK_VERSION` 与 `GRAPH_SEED_PACK_DIGEST`（digest 用 `GraphSeedPack.test.ts` 里的 `digestAgentPackManifest` 算出，跑一次测试从失败信息里抄实际值即可），Core 侧 E2E 的假 provider manifest（`TinadecCore.Api.Tests/ToolChainEndpointTests.CreateManifest`）也要覆盖种子包工具面，否则准入报 `spawnable_template_tools_unauthorized`。规则③：保留 mutating 工具面的 binding 必须声明 `envelope.resources.write`。
- **`ELECTRON_RUN_AS_NODE` 继承防护**: 从 Electron 宿主（VS Code / CodeBuddy 等）的终端启动 dev 时，`ELECTRON_RUN_AS_NODE` 会随环境继承到 `electron.exe`，使其退化为纯 Node 运行时——`require('electron')` 只返回 `electron.exe` 路径字符串，`app`/`BrowserWindow`/`protocol` 全为 `undefined`，主进程在 `protocol.registerSchemesAsPrivileged` 处抛 `TypeError: Cannot read properties of undefined`，窗口永远不出现（`.cjs` 主进程本身没问题）。修复分两层：`scripts/dev.mjs` 的 `createSpawnOpts()` 从子进程环境里 `delete ELECTRON_RUN_AS_NODE` / `ELECTRON_NO_ATTACH_CONSOLE`（覆盖 `npm run dev` 路径），`electron/main.cjs` 在顶部检测 `require('electron')` 非对象或缺少 `app` 时直接打印修复指引并 `exit(1)`（覆盖直接用 `npm run electron -w @tinadec/desktop` 或裸 `electron .` 的路径；该变量只要存在即生效，设为空串不解除）。排查命令：`echo $env:ELECTRON_RUN_AS_NODE`。
- **dev 启动等后端就绪**: `scripts/dev.mjs` 在 `waitForVite()` 之后（与 Vite 并行）追加 `waitForBackend()`：轮询 Gateway `/api/v1/health` 直到 `200 + core_status:"ready"`，Gateway 不存在时退回 Core `/api/v1/health`（`name:"tinadec-core"`），默认上限 120s（`TINADEC_DEV_BACKEND_WAIT_MS`，`0` 跳过）。原因：Vite/Electron 约 2–10s 起来，而 `dotnet run` 冷启动常要 60s+，两者并行时窗口会在 Core 监听之前加载数据，Gateway 代理返回 502 `CORE_UNREACHABLE`（`TinadecGateway/src/coreClient.ts`），应用内表现为「系统状态 / 加载失败 / 加载数据失败 / Cannot reach Core at http://127.0.0.1:48731」。Gateway 在自身健康但上游不可达时返回 `503 + core_status:"unreachable"`，所以它是现成的就绪信号。
- **Stale nested Vue copies**: root `overrides` pin `vue`/`@vue/compiler-sfc` to `3.6.0-rc.7`, but a bare `npm install` can (re)create physical `apps/desktop/node_modules/vue@3.5.x` + `node_modules/@vue/*` nested copies that shadow the override and break ~25 component tests (`insertBefore` null, boundary/css-contract assertion failures). Fix: delete `apps/desktop/node_modules/vue` and `apps/desktop/node_modules/@vue`, then re-run tests — resolution falls through to the root 3.6.0-rc.7. Do not "fix" the failing tests themselves for this cause.
- `vite-plugin-vue-mcp` 在 Vite dev server 上暴露 MCP server（SSE，`http://localhost:5173/__mcp/sse`），供 AI 客户端读取组件树/状态/路由/Pinia。项目根 `.mcp.json` 已注册 `vue-mcp` 客户端；需先启动 dev server，再启动 Claude Code（或 `/mcp` 重连）。
- 该插件 peer 范围只到 Vite 6，故根目录 `.npmrc` 设 `legacy-peer-deps=true`。此模式下 npm 不自动安装 peer 依赖，因此 `react`/`react-dom`/`react-is` 已作为显式依赖保留，勿删除。
