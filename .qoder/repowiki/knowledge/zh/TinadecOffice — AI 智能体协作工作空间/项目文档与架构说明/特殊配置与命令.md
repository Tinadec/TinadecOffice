本地开发统一通过 `npm run dev` 同时拉起 Core (48731)、Gateway (48730) 与 Desktop/Vite (5173)；Windows 下需先清除 `Version` 和 `Ice-Version` 环境变量以避免 MSBuild 解析失败；独立测试使用 `dotnet test tests/TinadecCore.Tests/TinadecCore.Tests.csproj -v minimal` 与 `npm run test --workspaces --if-present`。