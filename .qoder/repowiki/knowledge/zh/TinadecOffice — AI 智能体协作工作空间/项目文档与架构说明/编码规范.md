- 文档采用英文标题 + 中文正文的双语风格，关键术语保留英文原文（如 readiness receipt、tool descriptor、material panel）。
- API 契约以 Markdown 表格形式描述端点、用途与返回值，保持与代码中 `/api/v1/*` 路由一致。
- 演进计划按 `YYYY-MM-DD-<slug>.md` 命名并置于 `superpowers/plans/` 目录，便于时间线检索。
- PowerShell 示例统一使用 `Remove-Item Env:*` 清理环境、`Invoke-RestMethod` 健康检查、`Get-NetTCPConnection` 端口验证的标准模式。