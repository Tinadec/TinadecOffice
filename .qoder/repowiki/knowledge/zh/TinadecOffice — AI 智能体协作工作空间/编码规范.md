- Core 是唯一状态权威，Gateway 与 Desktop 不保存业务状态，Desktop 仅通过 Gateway 访问 Core
- 所有跨进程 API 契约统一使用 snake_case 命名
- 写操作必须经过 Gateway 审批门，禁止绕过直接调用 Core
- .NET 项目按 foundation/modules/host 三层划分，C# 与 F# 策略内核共存于同一解决方案
- Tool 层通过 JSON stdin/stdout 标准输入输出协议与 Core 通信，由 Roslyn 源生成器自动注册