PowerShell（Windows 原生脚本，使用 `Write-Host` 彩色输出、`Security.Principal` 权限检测、`ConvertFrom-Json` 解析 JSONC）；Node.js ESM（`import { existsSync, spawnSync } from 'fs'/'child_process'`，通过 `spawnSync` 跨平台调用 `npx skills add`）。