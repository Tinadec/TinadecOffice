构建需启用 AOT 发布（`PublishAot=true`）；生成器输出到 `Generated/` 目录（`EmitCompilerGeneratedFiles=true`）；可选复制 `native/rg/rg.exe` 或 `native/rg/rg` 作为 ripgrep 二进制；Windows 沙箱支持两种内部模式：`WindowsSandboxSetup.RunSetup()` 初始化环境，`WindowsSandboxRunner.RunRunner()` 以受限账户运行命令。