启动：`bun start`（等价 `bun src/index.ts`）；开发热重载：`bun dev`（`bun --watch src/index.ts`）；构建：`bun build src/index.ts --target bun --outdir dist --minify`；测试：`bun test src` 或 `bun test src --watch`。关键环境变量：`TINADEC_GATEWAY_MODE`（local/cloud）、`TINADEC_CORE_URL`、`TINADEC_TOOL_RUNTIME_URL`、`TINADEC_GATEWAY_PORT`、`TINADEC_GATEWAY_AUTH_REQUIRED`、`TINADEC_GATEWAY_JWT_SECRET`、`TINADEC_GATEWAY_API_KEY`、`TINADEC_GATEWAY_CORS_ORIGINS`、`TINADEC_GATEWAY_TIMEOUT_MS`。