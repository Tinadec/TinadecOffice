# 2026-08-29

## TinadecCore 运行时 agent 健康度端到端验证（含一个真实 bug 修复）

**结论**：agent 主链路（planning → execution → 工具 → 审批 → supervision → finalization）真实可用，已在真实 HTTP + 真实 OpenAI SDK + 真实 TinadecTools 子进程下端到端验证通过（用本地假 OpenAI 服务端 127.0.0.1:48799 驱动，零真实 API key）。

**验证路径**：建 project(path=沙箱) → session（workspace default mode 自动满足，bootstrap 14 agent/7 mode 全 published）→ interactions(agent_mode=agent) → run 停在 awaiting_user → POST /approvals/{id}/decision 放行 → TinadecTools 子进程把 hello-from-agent.txt 真实写入沙箱 → supervision pass → completed。中途重启 Core 后 run/审批状态完好（持久化恢复验证）。Provider 配置入口：`PUT /api/v1/model-providers/{id}`（If-Match 要纯数字不带引号，`api_key` 字段写 SecretStore，整个 body 存为 model-config，含 base_url/protocol/model）。

**发现并修复的真实 bug（升级路径 schema 漂移）**：
- 现象：`POST /approvals/{id}/decision` 500，run 永卡 awaiting_user。
- 根因：`GovernanceNonceMaterial` 迁移（202608220008，SQLite/PG 两侧）是**空操作**；governance 表由 bootstrap 创建无迁移历史，8/22 前建的库的 `capability_leases` 永远不会补 `nonce_secret_reference` 列、也不会删遗留的 NOT NULL `nonce` 列 → 审批决策写 lease 必炸。
- 修复：`TinadecCore/Persistence/DbContextMigrationParticipant.cs` 的 `DbContextSchemaBootstrapper` 增加列级调和（ReconcileModelColumnsAsync）：对既有表补缺失的模型列（NOT NULL 带类型零值默认），并定向 drop 遗留 `capability_leases.nonce`。幂等、SQLite/PG 双通用。回归测试：`TinadecCore/tests/TinadecCore.Governance.Tests/SchemaReconciliationTests.cs`。
- lifecycle 侧 `approval_requests` 有真实迁移（202608220007）覆盖，无此问题 —— 不对称是定位关键。

**测试基线**：Core 219 个测试隔离运行 100% 通过；并行全量跑有 flake（ToolChain/FullDuplex/CliRuntime/TinadecToolsProcess 互扰，含 `awaiting_user→awaiting_approval` 状态机锐边 StorageLifecycleService.cs:1107）。Gateway bun test 44/44 绿。

**其它确认**：`/api/v1/tool-layer-readiness` 无 default workspace root 时 tool_count=0 是设计内 fail-closed（project path 是真实 root 来源）；`model-readiness` 恒 provider_count=0 是已知桩；AGENTS.md 里"14 个 NotificationIslandHost 失败"已过期（已 describe.skip）。
