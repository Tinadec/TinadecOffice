> **⚠️ 历史文档（不可作为事实源）**
> 本文是当时由 AI 生成/执行的计划或设计稿，**未随代码更新**：其中的文件路径、路由名、数量统计与"已完成"结论均可能已失效。以源码、测试与 `AGENTS.md` 的 M 段记录为准。归档核对时间：2026-09-07（commit 3e8ff30）。

# 添加 Claude Messages 协议与 OpenAI Responses 协议支持（全栈）

## 现状
模型调用唯一缝隙是 `IAgentChatClientFactory`（DmaEA），实现固定走 OpenAI SDK chat completions。`ChatResolution` 无协议字段；`Driver` 字符串不参与客户端构造。Desktop 的 anthropic 模板保存后实际按 OpenAI 协议调用，不可用。`Microsoft.Extensions.AI.OpenAI` 10.8.0（已锁定版本）已内置 `AsIChatClient(ResponsesClient, string)`，Responses 协议零新增依赖。

## 设计决策
- 协议值（snake 风格字符串常量，集中在 Abstractions 新类 `ChatProtocols`）：`openai-chat`（默认）、`openai-responses`、`anthropic-messages`。
- 协议来源优先级：provider 配置 JSON blob 中的 `protocol` 字段 > 按 `Driver` 推断（`anthropic`→anthropic-messages，`openai-responses`→openai-responses，其余→openai-chat）。现有配置零迁移。
- Anthropic 方案：`Anthropic.SDK` 5.10.0 + 其 M.E.AI 适配包（实施时先 `dotnet package search` 验证适配包 ID，如 `Anthropic.SDK.Extensions`；若不存在则在 DmaEA 内写约 150 行的薄 `AnthropicChatClient : IChatClient` 适配器作为回退，映射 messages/tool_use/tool_result 与流式）。
- 不改 DB schema：`protocol` 存于现有不可变配置 JSON blob（`SaveProvider` 已原样存储整个输入 JSON）。

## Core 层（TinadecCore）
1. **`Directory.Packages.props`**：新增 `Anthropic.SDK` 5.10.0（及验证后确认的适配包）版本钉扎。
2. **`Abstractions/Ports/IModelProvider.cs`**：`ChatResolution` 增加 `string? Protocol`；同目录新增 `ChatProtocols` 常量类。
3. **`Models/ModelsModuleRegistrar.cs` `ModelProvider.ResolveChatAsync`**（约 :98-117）：从配置 JSON 读 `protocol`，缺失时按 `provider.Driver` 推断，写入 `ChatResolution.Protocol`。
4. **`DmaEA/IAgentChatClientFactory.cs`**：`OpenAiAgentChatClientFactory` 改为协议感知（可更名 `AgentChatClientFactory`），`Create(resolution)` 按 `Protocol` 分支：
   - `openai-chat`：现有 `OpenAIClient.GetChatClient(model).AsIChatClient()`（`PlanningAgent.DefaultChatClient` 保留）；
   - `openai-responses`：`new OpenAIClient(...).GetResponsesClient(model).AsIChatClient(model)`；
   - `anthropic-messages`：Anthropic SDK → `IChatClient`（base_url 规范化：SDK 若自带 `/v1` 前缀则剥离用户配置中的 `/v1`，避免双前缀）。
   同步更新 `DmaEAModuleRegistrar.cs:26` 注册。`PlanningAgent/SupervisionAgent/ExecutionAgent/FullDuplexRunEngine` 均经工厂取客户端，无需改动。
5. **`DmaEA/TinadecCore.DmaEA.csproj`**：新增 `Anthropic.SDK`（及适配包）PackageReference。
6. **`Api/Endpoints/ControlPlaneEndpoints.cs:16`**：模板端点从 1 个扩为 3 个，均带 `protocol` 字段：`openai-compatible`（openai-chat）、`openai-responses`（openai-responses，default model `gpt-5.4-mini`）、`anthropic`（anthropic-messages，default base_url `https://api.anthropic.com/v1`，default model 与 Desktop 模板一致 `claude-sonnet-4-6`）。
7. **`Runtime/ControlPlaneService.cs`**：
   - `ToProvider`（:44-49）：响应中透出 `protocol`（读配置 JSON，缺失按 driver 推断）；
   - `RefreshProviderModels`（:51-81）：anthropic-messages 分支改用 `x-api-key` + `anthropic-version: 2023-06-01` 请求头（Anthropic `GET /v1/models` 响应同为 `data` 数组，解析复用）；其余保持 Bearer。
8. **DevSeed 不动**（openai driver 默认 chat completions，行为不变）。

## Gateway 层（TinadecGateway/src/modelAgentCenter.ts）
- `SupplierResource`、`ApiConnectionResource` 及内部 `CoreProvider` 类型增加 `protocol: string | null`；supplier/provider 映射函数透传 Core 字段；`buildModelCenterSnapshot` 不做协议逻辑（薄代理原则）。
- `modelAgentCenter.test.ts`：新增 protocol 透传断言（supplier 模板与 api connection 各一）。

## Desktop 层（apps/desktop）
- **`src/api.ts`**：`ModelProviderTemplateDto`、`ModelProviderInstanceDto`、`SaveModelProviderInstanceInput` 增加 `protocol?: string | null`。
- **`src/providerTemplates.ts`**：`ProviderTemplate` 增加 `protocol: 'openai-chat' | 'openai-responses' | 'anthropic-messages'` 与可选 `protocols?: string[]`（支持的协议列表）；anthropic 模板→`anthropic-messages`，openai-compatible→`openai-chat` 且 `protocols: ['openai-chat','openai-responses']`，其余云/本地模板→`openai-chat`。
- **`src/pages/SettingsPage.vue`**：`providerForm` 增加 `protocol`；新建时按模板默认、编辑时回填 provider 现有值；模板支持多协议时在弹窗显示协议选择器；保存 payload（约 :1524）携带 `protocol`。
- **`src/locales/zh-CN.ts` / `en.ts`**：新增协议标签文案（Chat Completions / Responses / Claude Messages）。

## 测试与验证
1. Core 新增单测（AgentFramework.Tests）：协议分支→客户端类型正确；`ResolveChatAsync` 的 protocol 读取与 driver 回退推断。
2. Api.Tests：模板端点返回 3 模板且含 protocol 字段（可并入现有 control-plane 测试）。
3. `powershell -ExecutionPolicy Bypass -File scripts/setup-dotnet-env.ps1 restore/build/test TinadecCore/TinadecCore.slnx`（先停掉占用 48731 的 dev 进程），目标 80/80+ 全绿。
4. `npm --prefix TinadecGateway test`、`npm --prefix apps/desktop test`。
5. 安全基线：`dotnet list TinadecCore/TinadecCore.slnx package --vulnerable --include-transitive`（新增 Anthropic.SDK 后复查）。
6. 按 AGENTS.md 维护协议更新根 `AGENTS.md`（CURRENT REBUILD STATE + Last Updated 元数据）与 `TinadecCore/AGENTS.md` 相应段落。

## 风险与回退
- Anthropic M.E.AI 适配包 ID 未能在线确认（网络探测多次超时）：实施第一步即 `dotnet package search` 验证；不存在则启用薄适配器回退（已在设计中预留），不影响其余改动。
- Anthropic base_url 双 `/v1` 前缀：适配器内做规范化并加单测。
- Responses 协议在部分 OpenAI 兼容第三方端点不可用：协议是显式配置项，默认仍为 chat completions，不影响存量 provider。