> **⚠️ 历史文档（不可作为事实源）**
> 本文是当时由 AI 生成/执行的计划或设计稿，**未随代码更新**：其中的文件路径、路由名、数量统计与"已完成"结论均可能已失效。以源码、测试与 `AGENTS.md` 的 M 段记录为准。归档核对时间：2026-09-07（commit 3e8ff30）。

<analysis>
当前最后一公里的主阻塞点已经明确：Core 的 `POST /api/v1/sessions/{sessionId}/interactions` 是准入接口，只返回 `run_id`；Gateway 已经能代理它以及 `GET /api/v1/runs/{runId}/stream`；Desktop 的 HomeController 发送后没有用返回的 `run_id` 启动 durable run stream，因此即使 Core 完成了 meeting 响应，聊天界面也没有稳定的 assistant delta/done 消费路径。Desktop 还同时保留 HomeController、`useRunStream` 和 `stores/run.ts` 三套流式状态，旧 store 仍直连 `invoke-stream`，会造成入口和状态漂移。

本计划把 `interactions -> run stream -> meeting message -> UI projection` 收敛成唯一普通聊天路径，同时保留旧 `invoke-stream` 作为明确的兼容接口，直到引用和测试全部迁移。模型调用不使用假成功：手工验收需要通过现有 Model Center/SecretStore 配置真实 provider key；CI 使用已有 scripted chat factory 验证运行时协议。
</analysis>

<summary>
**实施目标**

完成一条可验证的普通对话闭环：Desktop 创建 interaction，Core 冻结 Agent/Mode/Prompt/Model/Tool/context 配置并后台运行，Gateway 代理 durable SSE，Desktop 只呈现 meeting 的用户可见响应，最终以 Core 持久化 message/run projection 校正界面；无模型密钥时必须显示结构化失败，不能伪造成功。

**P0：先锁定 Core 的真实运行契约**

1. 审核并补齐 `TinadecCore/DmaEA/FullDuplexRunEngine.cs` 的 `responding`/meeting 路径：确认 meeting 调用是否产生 run-stream `delta`，是否在终态持久化 assistant message，是否只对 meeting 产生用户输出，以及 `done`/`error` 后 Run 是否进入终态。
2. 若 assistant message 或 durable delta 缺失，在 Core 内补齐；Desktop 不通过兼容的 `POST /messages` 写 assistant 内容。响应的权威来源仍是 Core 的生命周期/会话存储。
3. 统一 stream chunk 的字段读取。Core 当前 endpoint 写的是顶层 `delta`、`error_category`、`safe_error_message`，Desktop parser 还兼容嵌套 `payload`。确定一个外部契约并在 Core、Gateway、Desktop generated DTO 中一致表达；如需标记输出归属，增加明确的 `agent_role: meeting` 或 `audience: user` 字段，客户端只接受 meeting/user-visible delta。
4. 修复 stream 终态行为：`done` 或 terminal `error` 后停止该订阅，不能被正常关闭逻辑重新连接；非终态断开继续使用 `Last-Event-ID`/`after_seq` 回放。保留订阅断开不取消后台 Run 的语义。
5. 审核 `TinadecCore/Api/Endpoints/InteractionsEndpoints.cs`：移除模式绑定失败的空 `catch`，让 session 的精确 ModeVersion 绑定失败时 admission 明确失败；严格校验 tenant/workspace 和 published/managed ModeVersion。核对 `fixed`/`parent_select` 是否把完整 provider/model/protocol 解析结果带入 frozen chat resolution，而不是只保存 model 字符串。
6. 让模型 readiness 与真实配置一致。`TinadecCore/Models/ModelsModuleRegistrar.cs` 当前 `CheckReadinessAsync` 为 scripted test 返回 assumed-ready；保留测试替身注入能力，但生产 readiness 必须反映 route、provider、base URL、protocol 和 SecretStore key 的缺失。错误统一映射为可识别的 `model_not_configured`/`run.failed` 语义。

**P0：Desktop 普通聊天只保留一套传输**

1. 修改 `apps/desktop/src/controllers/HomeController.ts`：
   - 继续使用 `api.createInteraction` 作为唯一 admission；成功拿到 `run_id` 后立即连接 `/api/v1/runs/{runId}/stream`，不再等待 event feed 代替 run stream。
   - 处理 `queued`、`parallel` 和 `insert`：只要有 `run_id` 就建立跟随；无 `run_id` 的 queued 项保留本地队列，并通过事件/运行列表重新发现后再跟随。
   - 为每个 active run 保存 stream handle、cursor、累积 meeting 文本和 terminal 状态，支持并行 run，不让一个 run 的 delta 覆盖另一个 run。
   - `ack` 建立运行状态；meeting `delta` 更新 assistant streaming projection；`done`/`error` 触发一次消息、run、orchestration、approval 的持久投影刷新并清理 pending placeholder；网络断线只暂停 reader，不调用 Core cancel。
   - 选中 session、切换页面和 controller 重启时，从 Core 的 active runs 重新 attach，cursor 从 0 或已保存 cursor 回放，依靠 `run_id + seq` 去重。
   - 保留现有用户错误状态，但优先使用结构化 `code/error_category/safe_error_message/trace_id`，覆盖模型未配置、权限、上下文冲突、恢复中、断网和普通失败。
2. 修改 `apps/desktop/src/composables/useRunStream.ts`，使其成为唯一的 GET run-stream 实现：支持动态 run attach、CRLF/分片边界、最后一个 SSE block、顶层和嵌套兼容字段、heartbeat cursor 更新、terminal stop、重连 cursor。将随机退避只保留在重连策略中，不影响事件顺序和去重。
3. 评估把 `useRunStream` 的纯传输部分提取为无 Vue 生命周期依赖的 `createRunStream`，由 HomeController 管理多个 run，Vue composable 只负责单个组件生命周期包装。这样不会在 module-level HomeController 中错误调用 `onUnmounted`，也避免为每个并行 run 重复手写 fetch/parser。
4. 收敛 `apps/desktop/src/stores/run.ts`：迁移仍有调用者到 interaction admission + run-stream follow；若确认没有生产调用者，将旧 `invoke` 标记为兼容路径并删除重复的 SSE reader/状态逻辑。保留兼容 API 时必须注明它不是 Home 普通聊天入口，避免两套状态同时写入。
5. 在 `apps/desktop/src/api.ts`/`src/generated/client.ts` 统一 `SessionInteractionDto`、`SseChunk`、run status 和错误 DTO；如果外部 OpenAPI 契约发生变化，同步生成/快照，不在 Gateway 或 Desktop 重算 Core 状态。

**P1：Gateway 契约和代理验证**

1. 核对 `TinadecGateway/src/index.ts` 的 interaction、run-stream 和 events 三条代理路径：interaction 原样返回 `run_id/turn_id/status/mode_version_id/meeting_model`，run stream 原样透传 SSE body，并正确把 `cursor`/`after_seq`/`Last-Event-ID` 转为 Core 查询。
2. 核对 `TinadecGateway/src/mappers/sseMapper.ts` 与外部 OpenAPI 描述。代理不能过滤未来扩展 kind；至少保证 `ack/delta/done/error/heartbeat` 以及 `task_node_update/supervision_update/context_version_update` 的字段不丢失。若 mapper 只用于非流式映射，则删除过时的“过滤”假设并补测试。
3. 更新 Gateway proxy tests 和 OpenAPI snapshot，覆盖：admission 返回 run identity、首次从 cursor 0 回放、断线用 `Last-Event-ID` 恢复、未知扩展 kind 原样通过、Core 4xx/5xx 映射为 ProblemDetails。

**P1：模型与 Pack 的手工前置条件**

1. 复用现有 Settings/Model Center API，不新增 Gateway 状态：确认 provider 已启用、provider version 有 `base_url`/protocol/model、API key 通过 SecretStore 保存、`chat` route 指向正确 provider/version/model。
2. 确认 OfficeAgentPack `tinadec.office.agent-pack@0.2.0` 已安装且默认模式和六个 `conversation.*` published/managed ModeVersion 可读；发送框选择的 `agent_mode` 必须解析到该版本。未安装或模式缺失时在发送前显示可操作错误，不能静默回退到旧 planner/executor。
3. 保持 `operation`/`execution` 公开层值和 meeting-only user output；`planning` 只保留兼容读取，不能作为新冻结配置或 Desktop 展示值。

**测试矩阵**

1. Core：扩展现有 FullDuplex/Interaction API 测试，断言 admission 的 `run_id`、ModeVersion/meeting model/frozen hash；scripted chat 验证 `ack -> meeting delta -> done`、assistant message 持久化、重放与 cursor 恢复、只呈现 meeting、并行 run 隔离、terminal error；无 SecretStore key 验证 `run.failed` 和 `model_not_configured`，没有 done-success 假象。
2. Gateway：扩展 `runtimeProxy.test.ts`、interaction proxy 测试和 OpenAPI snapshot，验证不改写业务状态、不丢 SSE 字段、不吞错误。
3. Desktop：为 `useRunStream` 增加分片/CRLF/heartbeat/dedup/reconnect/terminal tests；为 HomeController 增加 mock admission + mock durable stream 测试，断言返回 run_id 后会 follow、delta 只进入 assistant projection、done 后 reload 持久消息、错误状态可见、断线不会 cancel。
4. 手工真实 E2E：停止占用端口的旧服务，启动 Core `48731`、Gateway `48730` 和 Desktop；通过 Settings 配置真实模型 key/route，确认 Pack 与 conversation mode，创建项目/session，发送一条短消息，断言 Network/日志和 UI 依次出现 admission/run identity、`ack`、meeting `delta`、`done`，刷新后 assistant message 仍存在；随后断开/重连 stream，确认从 cursor 回放不重复；用无 key 配置再次验证结构化失败。
5. 按项目命令运行定向 Core/Gateway/Desktop 测试，再运行完整 `npm test`/Core solution tests；遵守运行 Core 前停止 dev server，避免 dll 锁和真实 TinadecTools 进程争用。

**文档与项目记忆同步**

实现完成后同步：
- `docs/app-core-ui.md`：删除仍声称普通聊天依赖旧 fallback 的漂移描述，写清 `interaction admission -> run stream -> meeting message` 顺序和两套 SSE（run stream/events）边界。
- `docs/architecture.md`：将 normal-chat migration 从剩余工作更新为已完成，并记录断线回放/持久投影规则。
- `apps/desktop/AGENTS.md`：记录 HomeController 的 canonical transport、stream abort 不等于 run cancel、meeting-only message 规则。
- `TinadecCore/AGENTS.md`、`TinadecGateway/AGENTS.md`：仅在实际改变 Core readiness/interaction 或 Gateway stream 契约时更新，并刷新 metadata；同步 OpenAPI snapshots 和受影响测试说明。

**验收门槛**

只有同时满足以下条件才算“第一句对话跑通”：真实模型配置可用；发送入口返回 durable `run_id`；Desktop 自动跟随 run stream；UI 只显示 meeting 的 assistant delta；Core 持久化 assistant message/run terminal projection；刷新或断线恢复不会重复；无 key 时以结构化失败结束而不是假成功。所有代码修改和验证均在计划获批后进行。
</summary>