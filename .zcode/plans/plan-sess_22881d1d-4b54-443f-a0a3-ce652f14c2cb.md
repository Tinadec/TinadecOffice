> **⚠️ 历史文档（不可作为事实源）**
> 本文是当时由 AI 生成/执行的计划或设计稿，**未随代码更新**：其中的文件路径、路由名、数量统计与"已完成"结论均可能已失效。以源码、测试与 `AGENTS.md` 的 M 段记录为准。归档核对时间：2026-09-07（commit 3e8ff30）。

本次仅完成只读诊断，不实施代码或配置变更。结论将区分：
1. 工作区是否存在显式 hooks 配置；
2. `.mimosa` 中记录的实际 PreToolUse/PostToolUse/Stop 安全审计行为；
3. Ponytail 插件所称的生命周期注入；
4. 不应误认为 hooks 的 npm 生命周期脚本和 MCP 配置。
后续若需要修改钩子配置，再单独制定实施方案。