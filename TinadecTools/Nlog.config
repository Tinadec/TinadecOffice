<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      throwConfigExceptions="true">
    <targets>
        <target
                xsi:type="Console"
                name="console"
                layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
                stdErr="true"
                autoFlush="true"
        />
        <!-- File logs must not land in the user's workspace (CWD = project root,
             would dirty git status); ${tempdir} renders Path.GetTempPath(). -->
        <target name="file" xsi:type="File"
                fileName="${tempdir}tinadec-tools/logs/${shortdate}.log"
                layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
                archiveFileName="${tempdir}tinadec-tools/logs/archive/${shortdate}.{#}.log"
                archiveAboveSize="10485760"
                maxArchiveFiles="30"
                keepFileOpen="false"/>
    </targets>
    <rules>
        <logger name="*" minlevel="Debug" writeTo="console"/>
        <logger name="*" minlevel="Info" writeTo="file"/>
        <logger name="Microsoft.*" minlevel="Warn" writeTo="console" final="true"/>
    </rules>
</nlog>
